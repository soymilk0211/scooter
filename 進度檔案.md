# 進度檔案

**這裡回答一個問題：現在到底能不能導航，還差什麼。**

長期的設計與踩過的坑在 [HANDOVER.md](HANDOVER.md)，待你拍板的問題在
[決策檔案.md](決策檔案.md)。這裡只講進度。

日期：2026-08-18

---

## 開新對話時把這段貼給我

```
這是 scooter 專案（台灣機車導航 App，公開 repo：github.com/soymilk0211/scooter）。

先讀這四份，順序不要換：
1. 進度檔案.md —— 現在到哪、還差什麼
2. 決策檔案.md —— 我已經拍板的與還沒拍板的
3. CONTEXT.md —— 術語，用它的詞不要換同義詞
4. docs/adr/ —— 0011～0017 是最近這幾輪的決定，0016 與 0017 最重要

已經知道的事，不要重新研究：
- 路線引擎是 BRouter，不是 Valhalla（Valhalla 沒有 Android 函式庫，ADR-0016）
- BRouter 不給路名也不給 OSM way 編號，那是它的圖磚格式限制，自建圖磚也解不了
- 圖磚是我們自己建的，注入在建圖之前，BRouter 的程式一行都沒改
- 路名走我們自己的 Overpass 管線，索引已經在種子庫裡了
- 車道級禁行機車不收集，官方有公告的規則與清單（HANDOVER 第一節 1b）
- 資料一律由用路人建檔，我不再手動建檔

工作方式：
- 用繁體中文，API 名稱保留英文
- 直接 commit 到 main 並 push，不要開分支問我要不要 merge
- 推之前確認 pipeline/.env 沒被追蹤（repo 是公開的）
- 需要我決策的寫進決策檔案.md 然後跳過，做完一輪再一起問我
- 進度寫進進度檔案.md
- 不要中途停下來問，做完再彙報

自建圖磚已經做完了（ADR-0017，管線在 pipeline/tiles/）。禁止左轉注入之後
路線真的會繞開，實測 6,398 → 6,627 公尺。待轉沒有生效，卡在 profile 的
成本模型，不在圖磚 —— 決策檔案 D10。

下一件事：把圖磚推上 GitHub Releases（進度檔案裡有指令，這台機器沒有 gh），
然後看 D10 要不要換成本模型。
```

## 一句話

**路線算得出來、轉向指示讀得到了，但還沒接成一個會導航的畫面。**
而且**整條路徑一行都沒在真機或模擬器上跑過**。

---

## 導航這條路，逐塊的狀態

| # | 塊 | 狀態 | 備註 |
| --- | --- | --- | --- |
| 1 | 路線引擎選型 | ✅ | BRouter（ADR-0016）。Valhalla 的 Android 函式庫不存在 |
| 2 | 引擎嵌入 | ✅ | `-ro.jar` 346 KB 進 `app/libs/`，JVM 實測 279 ms |
| 3 | 白牌 profile | ✅ | `scooter-tw.brf`。國道禁行**實測驗證**：對照組命中 110 條、我們 0 條 |
| 4 | 圖磚下載 | ✅ 程式 / ❌ 驗證 | `SegmentDownloader`，兩塊 20 MB（自建），Range 續傳、抓不到退回官方。**沒在裝置上跑過** |
| 5 | 檔案安裝 | ✅ 程式 / ❌ 驗證 | `BrouterFiles`，assets → filesDir。**沒在裝置上跑過** |
| 6 | 算路線 | ✅ | `ScooterRouter.route(from, to)` → 幾何 + 距離 + 轉向清單 |
| 7 | 轉向指示 | ✅ | `VoiceHintAccess`（放在 `btools.router` 套件裡，刻意的） |
| 8 | 目的地輸入 | ✅ 暫定 | **長按地圖選點**。決策檔案 D1 還沒定案，但這個做法不需要任何後端，先做著 |
| 9 | 路線畫上地圖 | ✅ | 琥珀 `#FFB020`，長按之後就會出現 |
| 10 | 導航跟隨 | ✅ | `RouteFollower`（core-rules，6 條測試）。記游標不做全域搜尋 —— 路線繞回起點附近時全域搜尋會讓進度倒退 |
| 11 | 轉向播報 | ✅ 暫定 | `ManeuverAnnouncer`（core-rules，6 條測試）+ `AlertPhrases.maneuver`。**不講路名**（D5 暫定 A），走即時 TTS；句子已經是有限集合，改成預合成只要動 `AlertVoice.speakManeuver` |
| 12 | 自建圖磚 | ✅ | `pipeline/tiles/`。20.3 MB，四組起訖驗收過 |
| 13 | 回報的禁止左轉進路線 | ✅ | 注入成轉向限制，實測路線會繞開 |
| 14 | 待轉成本進路線 | ⚠ | 圖磚那半做完了，**卡在 profile 的成本模型**，決策檔案 D10 |

**「能真正導航」的每一塊都接上了。** 長按地圖 → 算路線 → 畫成琥珀色 →
跟隨器算進度 → 20 秒／5 秒兩段播報。**但整條鏈只有編譯與單元測試，
沒有在任何裝置上跑過。**

**回報現在改變得了路線** —— 禁止左轉會被烘進圖磚，路線繞得開（ADR-0017）。
剩下的不是功能而是驗證與決策：真機、把圖磚推上 GitHub Releases、
以及待轉成本卡住的那個成本模型（D10）。

---

## 這一輪做完的事（第七段：自建圖磚）

- **自建圖磚做完了，而且回報真的改變得了路線。** 管線在 `pipeline/tiles/`，
  五分鐘跑完（含下載 326 MB 的台灣 OSM）。實測：注入一筆禁止左轉之後，
  台北車站到市府從 6,398 公尺變成 6,627 公尺，**其餘三組起訖一個位元都沒變** ——
  注入是外科手術式的，不是整體擾動。
- **一行 BRouter 的程式都沒改。** 注入全部發生在建圖工具看到資料之前：
  我們產出一份改過的 `.osm.pbf`，後面三步跑官方原封不動的
  `OsmFastCutter` → `PosUnifier` → `WayLinker`。理由是 BRouter 會發新版，
  而「我們改過它的內部類別」這種相依性會在升級那天才爆炸。
- **pbf 不整份重寫。** 它是一串各自獨立的 blob，沒動到的原封搬過去，
  只有裝了要切的 way 的那幾個才解開重編，最後補一個裝合成關聯的 blob。
  326 MB 跑完兩趟 8 秒。
- **高程整套不需要。** profile 一個 `uphillcost` 都沒指定，高程只改變回報的
  時間與能量、不改變路線 —— 自建（無高程）與官方（有高程）跑同一組起訖，
  距離一模一樣。省掉一整套 SRTM 的取得與前處理。
- **圖磚比預估小：20.3 MB 對官方 33.5 MB。** 官方那兩塊是整個 5×5 度網格
  （含福建沿岸、琉球），我們只有台灣。
- **待轉卡住了，而且卡的地方不在圖磚。** 標籤進得了圖磚（實測拿 `costfactor`
  擋那條 link，路線確實改變），但 `scooter-tw.brf` 繼承了 car-fast 的
  `---model:KinematicModel`，而 **`KinematicPath` 整支沒有呼叫過 `getTurncost()`**。
  刪掉那行也不行 —— car-fast 的 `costfactor` 對一般道路是 0，改走 StdPath
  距離就變免費。寫成決策檔案 **D10**，要你拍板。
- `SegmentDownloader` 改抓 GitHub Releases，抓不到才退回 `brouter.de`
  （決策檔案 D8）。**兩者不可混用**：一塊自建一塊官方，症狀會是
  「有時候會繞過那個禁止左轉，有時候不會」，比純官方難查得多。

## 這一輪做完的事（第四段：你拍板之後）

- **D2 做完了**：地圖方向可切換（北方朝上 ⇄ 車頭朝上），按鈕在左上角，
  設定會落地。手動旋轉手勢**仍然關著** —— 這是程式驅動的旋轉，
  騎乘中誤觸把方向轉歪比沒有旋轉更糟。
  方位角變化小於 5 度不轉，否則 GPS 抖動會讓整張地圖持續微晃。
  停止時（bearing 為 null）維持不動 —— 停紅燈時地圖跟著車身晃最沒有意義。
- **D4 + D6 寫成 [ADR-0017](docs/adr/0017-build-our-own-routing-tiles.md)**：
  自建機車專用圖磚，把回報的禁止左轉與待轉成本注入進去。
  這推翻了我「先只播報」的建議 —— 你說得對，回報改變不了路線，
  「越來越聰明」就只是一句話。
- 其餘（D1 的分享連結、D3 的吸附＋虛線、D5 的預合成、D7 的路名索引）
  還沒做，見下面「接下來的順序」。

## 這一輪做完的事（第六段：固定句預合成）

- 預合成的鍵從 `TurnRule` 換成 **`Phrase(key, text)`**，於是任何固定句都收得進來。
  現在有 **21 句**：左轉規定 6、轉向確認短句 2、測速 13（速限 6 種 × 超速與否 + 無速限）。
- **測速也改成預合成**了。先前判斷「不值得」是在還沒決定全部預合成的時候，
  而它順帶解掉一個實際問題：測速走即時、待轉走音檔，兩者響度可能差一截，
  騎士會以為「有些警示比較小聲」。
- **罕見速限（不在 30/40/50/60/70/80 裡）退回即時合成。** 測速的時窗是彈性的，
  慢三秒不影響；為了幾個罕見值撐大快取才是不划算的。
- 加了一條測試守「App 會去查的每一個 key 都真的在預合成集合裡」——
  **查一個不存在的 key 不會崩潰也不會編譯錯誤，它會安靜地退回即時合成**，
  而那正是「五秒前那則遲到三秒」的樣子。

## 這一輪做完的事（第五段：路名索引）

- **路名索引做完了**，而且已經在播報裡用上。
  臺北 13,102 條有名字的路 → Douglas-Peucker 簡化（容差 8 公尺，
  遠小於比對容忍量）→ **79,378 段降到 20,296 段**。
  種子庫 224 KB → **1,452 KB**，換到全臺北的路名。
- **座標存微度整數不存 REAL**：REAL 固定 8 位元組、整數會壓到 4，
  而微度約 0.11 公尺遠比 GPS 精確。兩萬段乘四個座標，這個選擇差一兩 MB。
- **查路名一定要帶方位角**。路口上兩條路交會，離騎士最近的線段可能是橫向那條，
  沒有方向就會把「忠孝東路」講成「復興南路」—— 那比不講路名更糟，
  騎士會照著錯的路名去找路口。所以 `Maneuver` 多了 `exitBearingDegrees`。
- **比軸線不比正反向**：OSM 的 way 方向是繪製時的節點順序，
  與騎士的行進方向無關，一條南北向的路可能被畫成由北往南。
- **D5 因此修正了一次**：路名讓句子變無限，與「全部預合成」直接衝突。
  改成 20 秒那則帶路名走即時 TTS（它有 20 秒可以吸收延遲）、
  5 秒那則固定句預合成（它是唯一有硬期限的）。

## 接下來的順序（照相依關係，不是照大小）

1. ~~路名索引~~ **✅ 完成**
2. ~~固定句預合成~~ **✅ 完成**。21 句（左轉規定 6、確認短句 2、測速 13）。
3. ~~自建圖磚~~ **✅ 完成**（ADR-0017）。禁止左轉會改變路線了。
4. **把圖磚推上 GitHub Releases**。檔案已經建好在
   `pipeline/build/tiles/segments4/`，**但這台機器沒有 `gh`，我推不上去** ——
   指令在下面「還沒做完的」。App 的網址已經指過去了，所以在推上去以前，
   下載會退回 `brouter.de`（功能正常，只是回報影響不了路線）。
5. **換掉 profile 的成本模型**（D10）。待轉成本卡在這裡，而且順帶會修掉
   「用一台 1,640 公斤汽車的動力學幫機車算路線」這件事。
6. **目的地吸附 + 虛線尾段**（D3）。
7. **Google Maps 分享連結**（D1 的另一半）。

### 還沒做完的：把圖磚推上去

```bash
gh release create tiles-2026.08 pipeline/build/tiles/segments4/E120_N20.rd5 pipeline/build/tiles/segments4/E120_N25.rd5 --title "路網圖磚 2026.08" --notes "台灣白牌機車專用 rd5 圖磚，OSM 快照 2026-08-17。"
```

標籤名 `tiles-2026.08` 必須與 `SegmentDownloader.BASE_URL` 裡的一致，
改一邊就要改另一邊。

## 這一輪做完的事（第三段：換到回報功能）

- **`TurnRule.NO_LEFT_TURN`**（需求第 5 條）。回報列多一顆按鈕，
  **單獨一列、琥珀底**，與上面四顆分開 —— 它回答的是「能不能轉」不是「怎麼轉」，
  排在同一列會讓它看起來只是第五個選項。
- 加這個 enum 值時，Kotlin 的窮盡 `when` 立刻在兩個地方編譯失敗
  （`AlertPhrases`、`AlertBanner`）。**那是好事** —— 兩處都是「這條規則要播什麼」，
  漏掉會變成靜默的錯誤播報。
- 新增規定**不必動 `AlertPhrases.VERSION`**：快取檔名帶 rule id，
  舊的四句沒改就仍然有效。VERSION 是給改既有文案用的。
- 發現一條新的卡點寫進決策檔案 **D7**：回報列的「XX路 ➔ YY路」拿不到路名，
  連帶「兩條離開路就把按鈕裂成兩顆」也做不到。與 D5 同一個根。

## 這一輪做完的事（第二段）

- `RouteFollower` —— 跟著路線走，記游標不做全域搜尋。
- `ManeuverAnnouncer` —— 20 秒主指示、5 秒確認短句，每則各播一次。
  塞車時有速度下限，否則「還有幾秒」會變成幾百秒而永遠不播。
- 長按地圖選目的地 → `ScooterRouter` → 琥珀色路線 → 播報，整條接起來。
- **播報不含路名**（D5 暫定），所以句子是有限集合 ——
  即時 TTS 那 2.8–3.6 秒的延遲問題可以靠預合成消掉，還沒做但路徑已經留好。

## 這一輪做完的事（第一段）

- `VoiceHintAccess`（Java，放在 `btools.router` 套件）—— 讀得到 `cmd`、`angle`、
  `distanceToNext`、`indexInTrack`、way 標籤。**這是 HANDOVER 說的「唯一真正的
  整合成本」，已經付掉了。**
- `ScooterRouter` 回傳 `Maneuver` 清單，帶沿路線的累積距離（導航跟隨要的是
  沿路走多遠，不是直線距離）。
- 更正了一個我自己寫錯的說法：**BRouter 不帶 OSM way 編號**，
  所以 ADR-0006 的事後驗證只能用幾何 —— 而 `ProhibitedMatcher` 本來就是幾何的。

## 兩個新發現，都寫進決策檔案了

- **D5：BRouter 不給路名。** 播報講不出「忠孝東路左轉」。
  但沒有路名代表句子是**有限集合**，於是全部可以預先合成 ——
  即時 TTS 那 2.8–3.6 秒的延遲問題整個消失。這是個好消息不是壞消息。
- **D6：`way_ids` 目前沒有用途**，留著是為了將來自建圖磚。

---

## 沒有驗證的東西（照風險排序）

1. **真機。** 從頭到尾沒有。GPS 精度、ROM 背景清殺、太陽下過熱。
2. **`SegmentDownloader` / `BrouterFiles`** —— 只有編譯通過。
   下載 20 MB、續傳、assets 複製，這三件在 Android 上都有各自會壞的方式。
3. **`ScooterRouter` 在 Android 上**。路由核心在 JVM 上實測過（279 ms），
   但 Android 的檔案路徑與 dex 化之後的行為沒試過。
4. **release 沒開 minify。** 要開的話 BRouter 的 expression 類別需要 keep 規則 ——
   profile 是執行期解析的，被混淆掉的症狀是「路線算不出來」不是編譯錯誤。
