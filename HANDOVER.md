# 交接文件

台灣機車導航 App。**自己做導航**，路線遵守白牌機車的路權，
路口的待轉規則**全部由用路人在現場建檔**。

先讀 [CONTEXT.md](CONTEXT.md)（術語）與 [docs/adr/](docs/adr/)（十六份決策紀錄，
**0011–0016 是這一輪的新決定，最重要**），本文件只講現況與尚未解決的事。

日期：2026-08-18

> **⚠ 產品範圍在 2026-08-18 第三次改變，這一次連資料來源一起換掉。**
>
> | 日期 | 決定 | 資料誰建 |
> | --- | --- | --- |
> | 08-16 早 | ADR-0007：做導航，與 Google Maps 正面對決 | 維護者 + 官方 |
> | 08-16 晚 | ADR-0010：收回成 Google Maps 的外掛 | 維護者 + 官方 |
> | **08-18** | **ADR-0011：做導航，且缺的那層資料由用路人建檔** | **用路人** |
>
> ADR-0010 否決導航的理由是車道級禁行機車覆蓋率 0%，沒有那層就會把騎士導上
> 禁行車道。**那個推論假設車道級禁行機車是路線問題 —— 它不是。**
> 內側車道禁行不會讓那條路變成不能走的路，騎士照走，只是走外側；
> 它改變的是路口能不能直接左轉，而那是轉向層的事。
> 把路線層與轉向層分開之後，路線層今天就做得對，轉向層今天不完整
> **但它的不完整是沉默不是錯誤**。
>
> 連帶效果：**手動建檔全部退場**（實地查核 13 筆、影像判讀 3 筆），
> 官方那 118 條規則**改當校準基準、不再對騎士播報**，
> 而 ADR-0008 的「沒有後端、每月 NT$0」被 ADR-0013 推翻 —— 現在有後端了。
>
> **出貨方式當天又改了兩次，最後收斂成：**
> **自己發 APK → 用路人建檔 → 覆蓋率夠高 → 封測 → 上架**（ADR-0015）。
> 原本要先出一個只收資料的建檔版、並先走 Play 封閉測試好讓上架時鐘開始跑；
> 改成**一次做到最終版，把 APK 放上網路讓有心人士下載去建檔**，
> **封測押後到覆蓋率夠高之後**。
>
> 這看起來只是發布管道，實際上動了兩件事：一是排序依據從「平台流程的前置時間」
> 換成「資料成熟度」；二是「未經審核的回報不會播進別人耳朵裡」這個保證
> 從架構層（沒有下行通道）移到了**發布閘門**，而閘門必須在第一個公開 APK
> 之前就完成。

## 現在做到哪裡（先讀這段）

**程式上：規則引擎、語音、測速、地圖、設定都在，導航與回報那一整塊都還沒開始。**
79 項單元測試全綠（core-rules 53、app 26），`./gradlew build` 通過，APK 53 MB。

**這一輪的文件已經改寫完**（本文件、CONTEXT.md、README.md、五份新 ADR），
**四個既有缺陷也修掉了**（見下）。導航、後端、審核那幾大塊還沒開始。

### 2026-08-18 修掉的缺陷

| # | 缺陷 | 為什麼會壞 | 修法 |
| --- | --- | --- | --- |
| 1 | **長紅燈之後取不到進入方位角** | `TrackBuffer` 的視窗只有 30 秒，而騎士停等時 GPS 仍每秒回報靜止點 —— 紅燈超過 30 秒，視窗裡就只剩速度 0 的點。**而長紅燈正是騎士會按回報的時候** | 方位角改成獨立持有，過期的判準從**時鐘**換成**位移**（人沒動，車頭朝向就沒變）。離開 50 公尺、或定位中斷超過 15 秒才作廢 |
| 2 | **騎乘中還能回報** | 解鎖門檻是單點速度 10 km/h。時速 9 公里還在滑行的騎士按下去，拿到的方位角可能正記在轉彎的半途 —— 規則會掛到一個不存在的來向上 | 改成「連續靜止 3 秒**且取得到進入方位角**」才成立，且**騎乘中整列不畫**而非變灰。兩個條件缺一不可 —— 只看停止的話，按鈕會在定位中斷後出現然後必定失敗，而騎士會以為是自己按錯，在只有幾秒的空檔裡反覆試 |
| 3 | **schema 遷移在騎乘途中跑，而且在主執行緒** | `RideService.engine` 是 `by lazy`，第一次開檔發生在定位回呼裡。開檔會複製種子資料庫（檔案 I/O）並跑遷移 —— 前者是 ANR，後者是「遷移失敗發生在最糟的時機」 | 改在 `onCreate` 用 IO 執行緒開，開好之前那幾秒不判定（那幾秒騎士一定是靜止的） |
| 4 | **塞車時時速圓圈無故變灰** | `AlertEngine.evaluate` 一進來就用 15 km/h 的警示門檻擋掉全部，連速限查詢一起擋。但速限是**持續存在的狀態**，不會因為騎士慢下來就消失 | 速限查詢與警示判定解耦；路口規則查詢仍然只在騎乘中做 |

### 2026-08-18 同時做掉的事

| 項目 | 內容 |
| --- | --- |
| **剛性時窗改成時間計** | 20 秒 × 當下時速，夾 80–350 公尺（需求第 2 條）。連帶把測速的彈性窗下緣綁到剛性窗上限，避免時速 60 以上兩個窗重疊 |
| **查清楚車道級到底怎麼做** | 官方有規則與清單，不必量也不必問。見第一節 1b。連帶訂正 ADR-0009 的一個用字 |
| **`import_prohibited.py` + `geocode_prohibited.py`** | 臺北市平面道路全面禁行機車 5 筆：解析、地理編碼、裁出折線與 way 編號，**4/5 可用**。剩下一筆的起點是「匝道入口處」不是路名。**尚未進種子庫**（見 1b） |

**79 項單元測試全綠**（core-rules 53、app 26），`./gradlew build` 通過。
`TrackBufferTest` 從 5 條加到 11 條，其中「停等 90 秒後仍取得進入方位角」
是這一輪的核心案例；`RuleMatcherTest` 加了三條守時窗的。

### 這一輪踩到的坑（每一個都花掉時間）

- **Overpass 用 `way(area.a)` 查標籤回傳 0 筆，換成 `way(bbox)` 同樣範圍回傳 774 筆。**
  那個 0 看起來像事實（「臺北沒有人標 `motorcycle`」），不像故障，我差點就把它
  寫進文件。**查 way 用 bbox，不要用 area。** 查 relation 用 area 是好的
  （轉向限制那次就正常）。
- **`data.gov.tw` 與 `data.taipei` 的搜尋 API 對 GET 回 405**，要 POST。
  找資料集用網頁搜尋比打 API 快得多，別在這上面耗。
- **這台機器的主控台是 cp950，Python 印中文會變亂碼。** 跑管線或臨時腳本
  一律加 `PYTHONIOENCODING=utf-8`。第一次量 OSM 覆蓋率時整份輸出讀不懂。
- **`TrackBufferTest` 裡有一條測試在守著那個 bug。** 「stale bearing must not leak
  through」把「30 秒後方位角要消失」寫成了刻意行為，而那正是缺陷本身。
  修的時候必須連測試的**意圖**一起改（過期的判準從時鐘換成位移），
  不是把測試刪掉 —— 它想守的東西是對的，只是守錯了維度。
- **`sed` 的全域比對會改到你沒看到的那一行。** 修一個 Markdown 表格欄位時
  `sed -i 's/…/…/'` 同時命中了兩行，兩行都變成亂碼。這與 HANDOVER 早就寫著的
  「不要用程式去修補程式」是同一條 —— 那條原本講的是 Python 改 Python，
  **`sed` 改 Markdown 也算**。要改就用精確的整段字串替換，改完看一眼。
- **改一個常數會靜默地弄壞另一個窗。** 剛性窗上限從 300 變成 350 之後，
  它與測速的 320 公尺下緣重疊了，而那是「兩則警示搶語音通道」——
  症狀會是偶爾漏播，不會是崩潰。**被 `EnforcementMatcherTest` 裡一條守不變式的
  測試接住了**，現在那個下緣直接綁在 `AlertThresholds.MAX_LEAD_METERS` 上，
  不再是獨立的魔術數字。
- **自己修的東西自己弄出新破綻。** 把回報改成「停止才顯示」之後，
  出現了「停住但方位角已失效 → 按鈕出現、按下去必定失敗」的路徑。
  停止與方位角**必須一起判**，否則騎士會以為是自己按錯，在幾秒的空檔裡反覆試。

`pipeline/build/` 是 gitignore 的，換一台機器要重跑管線（第五節）。
`.env` 的 `MAPILLARY_TOKEN` 與 `CGU_API_KEY` 也沒有進版控，要另外帶。

---

## 一、新需求逐條怎麼落地

這一節是這次改寫的重點。六條需求加上一個問題，逐條寫下定案與理由。

### 1. 導航必須遵守交通規則

**做得到的與做不到的要先分清楚**，否則第一版出貨會被誤認為需求達成。

| 要遵守的 | 第一版狀況 |
| --- | --- |
| 不走國道、白牌禁行的快速道路與高架 | ✅ 做得到。Valhalla 的 `motor_scooter` costing 實測避開國道（ADR-0006），另備禁行路段清單事後驗證 |
| 單行道 | ✅ OSM 覆蓋 97.1%（臺北實測） |
| 禁止左轉／右轉／迴轉 | ✅ 臺北市 OSM 有 **1,204 條**轉向限制關聯，Valhalla 原生支援 |
| 車道級禁行機車 | ❌ **臺北 1.1%、台中 0%**，而且**刻意不收集**（見下）。它不改變路線 |
| 待轉／直接左轉 | ⚠️ 只有已建檔的路口知道。沒建檔就不播，騎士回到他本來的預設 |

**車道級禁行機車連建檔都不做**（2026-08-18 決定）。原本打算交給用路人回報，
但騎士停在路口答不出「這條路內側車道禁不禁行機車」—— 那要看路面標線與號誌桿上
的小牌子，而他戴著安全帽、只有幾秒空檔。**回報太難的東西，收到的是雜訊不是資料。**

而且不需要：那件事唯一的作用是決定騎士到不到得了左轉專用道，也就是能不能直接
左轉 —— 回報「直接左轉」就代表到得了，回報「待轉」就代表到不了或現場要求待轉。
**收結果不收成因**，問題從一個看不懂的技術問題變成他每天都在做的判斷。

**達成需求的條件是建檔覆蓋率，不是程式完成度。** 程式可以在一個月內寫完，
覆蓋率要靠人騎出來。

兩個實作時一定會踩的細節（2026-08-18 實測 Overpass 得到的）：

- **分時段禁轉的寫法沒有統一。** 同一個城市裡同時存在 `@ 07:00-22:00`、
  `@07:00-22:00`、`@ (Mo-Fr 07:00-09:00)`、`@ (07:00-09:00, PH off)`、
  `@ 07:00-09:00,17:00-19:00`。這與匯入測速資料時「拍攝方向有十幾種寫法」
  完全是同一類問題，處理方式也一樣：**只解析能確定語意的那幾種，其餘當成全時段禁轉**。
  保守的方向在這裡是多繞路，不是違規。
- **只有 11 條關聯帶 `except=motorcycle`。** 資料裡幾乎沒記載「這個禁轉對機車例外」，
  所以一律當成對機車也禁。

**最容易犯的錯誤寫在 [ADR-0012](docs/adr/0012-hook-turn-is-a-cost-not-a-restriction.md)**：
不要把待轉編成 `no_left_turn`。那會讓路線引擎繞開整個路口，
騎士被導去繞三個街廓完成一個原地等 40 秒就能做完的左轉 ——
而且這個錯誤不會被回報成錯誤，路線看起來是合法的，只是莫名其妙地繞遠。
待轉是**成本**（一個號誌週期），不是禁止。

### 1b. 車道級到底要怎麼做（2026-08-18 查清楚了）

**結論先講：車道級禁行機車在臺北不是一份幾何圖資，是「一條規則 + 幾份例外清單」，
而那些清單官方全部都有。** 我們一直在找錯的東西 —— 找的是 OSM 的
`motorcycle:lanes`（臺北 1.1%、台中 0%），而該找的是交通局的公告。

**臺北市交通局明文的規則**：單向三車道以上的道路，內側車道禁行機車，
最外側兩車道供機車行駛。目的是減少汽機車交織。

搭配四份清單（[交管處機車行車管制](https://bote.gov.taipei/Content_List.aspx?n=80B2C2DD47DD853E)）：

| 清單 | 格式 | 我們的狀況 |
| --- | --- | --- |
| 三(含)車道以上**例外開放直接左轉**路口 | PDF / 已有 CSV | ✅ 已在用（118 條的來源之一） |
| 二車道路段**例外實施兩段式左轉**管制清冊 | PDF / 已有 CSV | ✅ 已在用 |
| **平面道路禁行機車**（全面禁行的路段） | **CSV，1 KB，5 筆** | ❌ 從來沒匯入過 |
| **開放機車行駛第 3 車道**路段列表 | **只有 PDF**（104 KB） | ❌ 沒有機器可讀的來源 |

#### 這件事改了三個既有的判斷

**一、ADR-0009 說「車道數推待轉是巧合不是推導」，這句話要修正。**
因果鏈是有文件的：三車道以上 → 內側車道禁行機車 → 騎士到不了左轉專用道 →
必須待轉。交通局自己就是這樣寫的。它**仍然是市府的行政實務而不是法條**
（法條是道安規則 §99，觸發條件是內側禁行或標誌），所以 ADR-0009 分兩層的結構
完全正確；錯的只是「巧合」那個字。**代理層是一條被公告過的政策，不是統計上的碰巧。**

**二、那 5 筆是路線層的資料，不是轉向層的。** 它們的意思是「這條路機車完全不能走」
—— 堤頂大道1段、大直橋往明水路匝道、環河北路、忠孝西路兩個方向。
這正是 [ADR-0006](docs/adr/0006-white-plate-scooters-only.md) 說的「另備一份機車禁行
路段清單，於路線計算後驗證」的那份清單，而它只有五筆、帶起訖點與方向，
**小到今天就能做完**。ADR-0006 當時假設這份清單要自己建，其實官方已經發布了。

**三、群眾建檔車道級的決定不變，但理由升級了。**
原本是「太難回報」（真的太難），現在還多一條：**官方已經有了，只是格式難用**。
花力氣去問一萬個騎士一個交通局已經公告過的答案，是最糟的一種眾包。

#### 那還缺什麼

缺的是**「開放機車行駛第 3 車道」那份清單只有 PDF**。管線目前零外部相依
（`config.py` 的 docstring 說明了為什麼），而解析 PDF 需要外部套件。三個選項：

1. **接受一個相依**（`pdfplumber` 之類）—— 破壞「clone 下來就能跑」。
2. **跟交通局要 CSV** —— 那四份裡有兩份已經是開放資料，這份沒有理由不能是。
   成本是等待，不是工程。
3. **先不做** —— 第 3 車道開放與否只影響**車道級指引畫面**（高德那種
   「請切到右邊第二車道」），而那是後期功能。它**不影響待轉判定**：
   內側禁行與否的結果已經由待轉回報直接回答了。

**建議走 3 加 2**：先不做，同時去要 CSV。不要為了一份只影響後期畫面的清單，
把整條管線的相依前提破壞掉。

#### 所以「車道級」的完整路線是

1. **現在**：不做。待轉回報已經回答了它唯一影響騎士的那個問題。
2. **路線層**：匯入那 5 筆全面禁行路段，當 ADR-0006 的安全網。
   **✅ 解析與折線都完成了**（`import_prohibited.py` + `geocode_prohibited.py`，
   4/5 可用），❌ 還沒進種子庫，見下。
3. **車道級指引畫面**（如果哪天要做）：規則（三車道以上內側禁行）+ OSM 的
   `lanes`（臺北覆蓋 68%）+ `oneway`（97%）+ 第 3 車道開放清單。
   **不需要 HD map，也不需要群眾建檔** —— 它是推導出來的，不是量出來的。
4. **其他縣市**：各縣市政策不同，要各自登錄（ADR-0009 的 `CITY_DEFAULTS` 結構
   已經為此準備好了）。沒登錄的縣市不推導，沉默是刻意的。

#### `import_prohibited.py`（2026-08-18 新增）

```bash
python import_prohibited.py --selftest   # 來向→面向的轉換
python import_prohibited.py              # 下載、解析、寫 build/prohibited_raw.json
```

解析出來的五筆：

| 行政區 | 路名 | 面向 | 起訖 | 速限 |
| --- | --- | --- | --- | --- |
| 內湖區 | 堤頂大道1段 | 180°（北往南） | 港墘路 → 南京東路 | 70 |
| 中山區 | 大直橋往明水路匝道 | 0°（南往北） | 匝道入口處 → 明水路 | 50 |
| 大同區 | 環河北路 | 180°（北往南） | 社中街 → 民生西路 | 60 |
| 中正區 | 忠孝西路 | 90°（西往東） | 重慶南路 → 中山北路 | 50 |
| 中正區 | 忠孝西路 | 270°（東往西） | 中山北路 → 重慶南路 | 50 |

**方向欄與待轉那三份 CSV 同一個慣例**（來向），編碼也一樣是 Big5 ——
同一個機關出的，這件事可以預期，別再重新試一輪編碼。

**刻意沒有重用 `taipei_parse.parse_direction`。** 那個函式會一併算出「左轉後的
離開方位角」，因為它服務的是路口規則。路段沒有離開方位角，硬套會在輸出裡多出
一個看起來有意義、其實沒有的欄位 —— **那種欄位遲早會被人拿去用**。

#### `geocode_prohibited.py`（2026-08-18 新增）

```bash
python import_prohibited.py && python geocode_prohibited.py
```

把那五筆解成折線。輸出 `build/prohibited.json`，每筆帶 `start`／`end`／
`polyline`／`way_ids`／`status`。**結果 4/5 可用：**

| 路段 | 直線 / 沿線 | 點 / way | 狀態 |
| --- | --- | --- | --- |
| 堤頂大道1段（北往南） | 2,263 / 2,725 m | 88 / 14 | ✓ |
| 環河北路（北往南） | 3,802 / 4,462 m | 77 / 12 | ✓ |
| 忠孝西路（西往東） | 654 / 870 m | 51 / 18 | ✓ |
| 忠孝西路（東往西） | 654 / 870 m | 51 / 18 | ✓ |
| 大直橋往明水路匝道 | — | — | ✗ 見下 |

座標抽查過：忠孝西路×重慶南路落在北門、×中山北路落在車站東側；
環河北路×社中街落在社子、×民生西路落在大稻埕；堤頂大道兩端都在內湖。

**為什麼要折線不要兩個端點**：堤頂大道沿線比直線長 20%、環河北路長 17% ——
用兩點連成的直線去判「騎士是不是在這條路上」，中段會偏出去一百多公尺，
而誤報的內容是「你正走在禁行機車的路上」，那是最不能亂講的一句話。

**`way_ids` 的用途已經更正（2026-08-18）**：我原本寫它是給路線引擎事後驗證用的。
**那是錯的 —— BRouter 完全不帶 OSM way 編號**，rd5 是重新編碼過的圖，
jar 裡查不到任何 `wayId`。所以事後驗證**只能用幾何**，也就是 `ProhibitedMatcher`
已經在做的點到折線距離 —— 那段程式的做法本來就是對的。
`way_ids` 留著是為了將來自建 rd5 圖磚時當注入的鍵（決策檔案 D4/D6），
不是現在的驗證鍵。

**忠孝西路兩個方向的折線完全相同，這是對的** —— 它不是分隔島道路，兩向共用
同一組幾何，方向由 `bearing` 欄位分開，不由幾何分開。

#### 寫這個模組時踩的兩個坑

**一、路名變體必須放進 Overpass 查詢，不能只在索引裡試。**
`trunk_ways_for` 會試「堤頂大道1段 → 堤頂大道一段」（OSM 台灣用中文數字），
但索引裡只裝得下查詢回傳過的東西。第一次跑把原始名稱丟進 `fetch_ways`，
於是索引裡根本沒有那條路，**症狀是「這條路查不到」，不是「名字對不上」**。
現在查詢前先用 `name_variants` 展開（12 個路名 → 22 個變體）。

順帶砍掉一個自己加的錯誤：我原本用路名尾字白名單（路／街／道／橋／巷／弄）
過濾掉「匝道入口處」那種非路名，而**那個白名單把「堤頂大道1段」也擋掉了** ——
它結尾是「段」。非路名留在查詢裡只是多幾個匹配不到的替代項，成本是零。

**二、分隔島道路的判定只設下限會被彎道誤觸。**
判定方式是把節點投影到起訖直線上，看兩側的平均側向偏移差；真正的分隔島道路
兩個方向相距一個中央分隔帶的寬度。**環河北路沿著淡水河彎過去，兩側偏移差
240 公尺**，被判成分隔島而丟掉了一半節點（第一次跑只拿到 39 點）。
加上 60 公尺的上限之後才對 —— 道路不會有幾百公尺寬。

#### 剩下那一筆：大直橋往明水路匝道

**起點欄寫的是「匝道入口處」，那不是路名，解不出座標。**
它也不可能靠用路人回報補上 —— 那條路禁行機車，騎士本來就不該出現在那裡。
只能有人去查一次座標。放在 `build/review_prohibited.csv` 裡。

**這是目前唯一還需要人工的一筆**，而它需要的是座標不是判斷，
與「放棄手動建檔」那個決定不衝突（那條決定講的是規則，不是幾何）。

#### ✅ 已經整條接起來了（2026-08-18）

| 層 | 內容 |
| --- | --- |
| 管線 | `build_seed.py` 寫入 `prohibited_segments` 與 `prohibited_cells`，**4 筆** |
| schema | **升到第 3 版**，`Schema.MIGRATIONS[2]` 純新增兩張表 |
| core-rules | `distanceToSegmentMeters` / `distanceToPolylineMeters`（Geo.kt）、`ProhibitedSegment` / `ProhibitedMatcher`（Prohibited.kt） |
| data | `ScooterDatabase.prohibitedNear()` |
| app | `RideAlerts.prohibited` → `RideRepository.prohibited` → `AlertVoice.speakProhibited()` |
| 地圖 | 紅色 `prohibited` 線圖層（**source 還是空的，見下**） |

**為什麼要 `prohibited_cells` 這張對照表**：一段路可能有好幾公里，橫跨十幾格
（一格約 1.1 公里，環河北路佔 5 格）。像點位那樣在主表放一個 `cell` 欄位只會
登記到起點那一格，**騎在中段的人查不到** —— 而症狀是「這條路有時候會警告、
有時候不會」，比完全不做更難查。

**判定門檻**：側向 25 公尺（道路半寬 + 都市峽谷 GPS 誤差）、方位角 ±45°、
速度 ≥ 8 km/h（比警示的 15 低 —— 塞車也還是在禁行路段上）、冷卻 5 分鐘。

**播報措辭刻意是提醒不是指控**：「XX 路禁行機車，請於下個路口改道」，
不是「你違規了」。理由在下一段。

#### ⚠ 這個判定的已知弱點，用之前要知道

它是**點到折線的距離 + 方位角閘門，不是 map matching**。所以它
**分不出垂直分離的道路**：環河北路的正上方就是環河快速道路，水平距離是零、
走向完全相同 —— 騎在高架上的人與騎在平面的人，在這個判定眼裡一模一樣。

目前**剛好不致命**，因為白牌本來就不能上那條快速道路，會被誤判的人本來就
不該在那裡。但同樣的結構在別的路段可能反過來（合法的平面道路被上方的禁行
高架蓋到），所以措辭不能是指控。真正的解法是路網圖上機後改用 map matching
（Valhalla 的 Meili），那時直接比對 way 編號，垂直分離不再是問題 ——
`prohibited_segments.way_ids` 就是為那一天存的。

#### 踩到的第三個坑：種子庫的版本號

**改了 `Schema.kt` 的 `SCHEMA_VERSION` 之後，`build_seed.py` 的同名常數也要改，
而且要重跑一次管線再複製。** 我先跑了 `build_seed.py` 才改常數，
結果種子庫的 `PRAGMA user_version` 是 2 但表結構已經是 3 ——
App 開檔時會以為要從 2 升到 3，於是對著已經存在的表跑 `CREATE TABLE`，
**直接拋例外**。抽查 `user_version` 才發現。

上線前的檢查清單要有這一條：`PRAGMA user_version` 必須等於 `Schema.SCHEMA_VERSION`。

### 2. 轉向指示與 20 秒提前量

**時窗改用時間算，不用固定距離。** 現行的剛性警示是寫死 300 公尺，
但同樣 300 公尺，時速 20 給騎士 54 秒、時速 50 只給 21.6 秒 ——
差了兩倍以上，而那正是提示是否來得及的關鍵。

定案：

| 提示 | 時機 | 內容 | 語音 |
| --- | --- | --- | --- |
| 待轉提示 | 約 **30 秒**前 | 「前方 OO 路口左轉需待轉，請保持外側」 | 動態 TTS |
| 主轉向指示 | 約 **20 秒**前（夾在 80–350 m） | 「前方 300 公尺，OO 路左轉」 | 動態 TTS |
| 確認短句 | 約 **5 秒**前（最少 25 m） | 「這裡左轉」／「待轉」 | **預合成 wav** |

**✅ 剛性窗已改成時間計（2026-08-18）。** `AlertThresholds.leadDistanceMeters(speed)`
＝ 當下時速跑 `LEAD_SECONDS`（20 秒）的距離，夾在 80–350 公尺之間。
排序本來就用 `AlertCandidate.secondsAway(speed)`，現在**入窗與排序用的是同一個量**。
上表的三段式（30 秒待轉提示、5 秒確認短句）還沒做 —— 它們要等導航，
因為沒有路線就不知道騎士會不會左轉，現在只能發一則條件句。

實際數字：時速 25 → 139 公尺，時速 40 → 222 公尺，時速 60 → 333 公尺。

**踩到並修掉的連鎖反應**：測速的彈性窗下緣原本是 320 公尺，而新的剛性窗上限是
350 公尺 —— 時速 60 以上兩個窗會重疊，那正是兩則警示搶同一條語音通道的情況。
`EnforcementThresholds.MIN_DISTANCE_METERS` 因此改成
`AlertThresholds.MAX_LEAD_METERS + 30.0`（380 公尺），**綁在剛性窗上而不是再寫一個
魔術數字** —— 這樣以後調 `LEAD_SECONDS` 不會又悄悄製造一次重疊。
`EnforcementMatcherTest` 本來就有一條測試在守這個不變式，改壞了它會紅。

**播報以停止線為準，不以路口中心（2026-08-18）。** 規則的座標是路口中心
（OSM 節點群聚的形心），但騎士要停下來、要做決定的地方是停止線，
大路口的停止線在中心前方十幾二十公尺。照中心算，每一則警示都晚一點點 ——
時速 40 是一秒多。`RuleMatcher` 現在把
`AlertThresholds.STOP_LINE_SETBACK_METERS`（15 公尺）從距離上直接扣掉；
騎士是沿進入方位角過來的（±30° 閘門保證），所以不必另外算一個停止線座標。

**為什麼是常數而不是每個路口各自的值。** 想過用 `junctions.json` 的
`node_count`（群聚了幾個號誌節點）推路口大小，但那個對應關係**我們沒有任何
方法驗證** —— 那會是一個編出來的校準，而編出來的校準比一個誠實的常數更難
發現是錯的。要做得更準該從 OSM 的 `lanes` 取橫向道路寬度（臺北覆蓋 68%），
那要新增欄位，等有辦法驗證再說。15 公尺的偏誤方向是保守的：
估太多警示提早，估太少警示遲到，而遲到就是騎士錯過路口。

**連帶要改粗篩半徑**：規則存的是中心，時窗量的是停止線，
所以中心在 360 公尺的路口，停止線已經在 345、落在窗內了。
`MAX_DISTANCE_METERS` 因此是 `MAX_LEAD_METERS + STOP_LINE_SETBACK_METERS`。
沒改的話症狀是「高速時偶爾不響」—— 有測試守著。

**一個誠實的註腳**：`MIN_LEAD_METERS = 80` 在目前的設定下**其實碰不到** ——
警示的速度門檻是 15 km/h，而 15 km/h 跑 20 秒就是 83 公尺，永遠大於 80。
留著它是因為速度門檻哪天為了建檔模式調低時，時窗塌陷會立刻回來，
而那種錯誤的症狀是「塞車時警示在路口正中央才響」，不容易聯想到門檻。

三件事解釋一下：

- **待轉提示要比轉向指示早**，因為它改變的是「要不要現在變換車道」，
  不是「要不要轉」。等到轉向指示才講，騎士可能已經切進內側 —— 而內側正是禁行機車的那條。
- **20 秒是上限不是常數**，要夾在 80–350 公尺之間。塞車時時速 5，
  20 秒只有 28 公尺，那句話等於在路口正中央才講；空曠時時速 60，
  20 秒是 333 公尺，再遠就會有中間路口混淆。
- **5 秒那一句必須是預合成的。** 即時 TTS 首句延遲實測 **2.8–3.6 秒**
  （第四節），二十秒吸收得了，五秒吸收不了 —— 那句話會在路口之後才播出來。
  所以動態語句（含路名）一律排在 20 秒那次，5 秒只講固定短句。

### 3. 停下才能回報，路口與方向由 GPS 推得

**✅ 地基已修（2026-08-18）** —— `TrackBuffer` 與停止判定都做完了，見「已修的缺陷」。
下面留著定案，尚未實作的是路名與路口那幾項。

已經在程式裡的：

- **停止判定**：速度 ≤ 3 km/h 持續 ≥ 3 秒（`TrackBuffer.isStopped()`）。
  低於這個門檻的短暫停頓不算，否則按鈕會在路口前一閃一閃。
- **回報列只在停止時出現**，不是變灰 —— 騎乘中根本不畫（`MainActivity`）。
  這比舊的「> 10 km/h 鎖定」嚴格，而且是對的：鎖定的按鈕仍然邀請人去按，
  而時速 9 公里還在滑行的騎士，方位角可能正記在轉彎的半途。
- **進入方位角在長紅燈之後仍然取得到**（`TrackBuffer.approachBearing()`）。
- **按鈕出現就一定按得成**（`RideRepository.canReport` 同時要求停止與方位角）。
  `ReportOutcome.NO_BEARING` 因此變成理論上的防呆，不是常態路徑。

還沒實作的（都要等路網圖上機）：

- **目標路口**：沿進入方位角**前方 40 公尺內最近的路口節點**。找不到就不顯示按鈕。
  這道限制擋掉「停在路段中間亂按」，也讓「只回報這個路口」這件事是幾何保證的，
  不是靠使用者自律。
- **按鈕上方那一行**：`<進入路名> ➔ <離開路名>`。左轉可達兩條不同的路時，
  箭頭另一側寫 `A & B`。路名來自路網圖（Valhalla 的圖磚本來就帶路名，
  不需要另外的地理編碼服務）。
- **每一筆建檔都帶路口座標**（使用者明確要求）。
  **2026-08-18 換了參考點：以停止線為主，不以路口中心為主。**
  騎士停等時的 GPS 位置**就是**停止線，所以 `RideViewModel.onReport`
  存騎士位置是對的 —— 這一項先前被記成「等路網圖才修得對」，
  在新的框架下它本來就對。路口中心是「停止線沿進入方位角再往前一個退距」，
  需要時反推得出來。

**`&` 只是標籤，不影響規則掛在哪裡。** 待轉與直接左轉的規則掛在
（路口座標, 進入方位角）上（ADR-0001），走哪一條離開路都一樣。

**但禁止左轉是例外，它必須指名是哪一條被禁。** 所以：

- 只有一條離開路 → 「禁止左轉」一顆按鈕。
- 有兩條離開路 → **裂成兩顆**，各自標上那條路的名字。
  這樣「按下當下立即送出」仍然成立，而歧義在按下去以前就被畫面消掉了，
  不需要任何確認步驟。

### 4. 按下當下立即回報

**不做確認畫面、不做復原。** 這個動作要在紅燈的幾秒內完成，
確認畫面會把一秒的操作變成三秒，於是騎士不做了。

「立即」的正確意思是**排隊立即、網路盡快**：回報先寫進本機 `observations`
（該表本來就有 `uploaded_at` 欄位，就是為了離線排隊而存在），
再由上傳器批次送出。逐筆連線在隧道與地下道會把電池吃光。

按錯的處理不是復原，是**同一裝置在同一路口同一進入方位角只算最新的一票**。
按錯了就再按一次對的，天然就是更正 —— 而且這條規則同時擋掉一個人按二十下定案一個路口。

### 5. 「禁止左轉」按鈕，但它的門檻要比別的高

新增這顆按鈕會讓導航變聰明，但它與其他按鈕**性質不同**：待轉與直接左轉改的是
播報，禁止左轉改的是**路線**。

一筆錯誤的禁止左轉會讓所有人在那個路口繞遠路，**而且不會有人抱怨** ——
繞遠的路線看起來仍然合法，騎士無從得知它為什麼繞。錯誤的待轉播報至少會被聽出來。

所以定案：**禁止左轉的回報一律進審核佇列，不論該裝置的正確率多高**，
且採用門檻高於其他規則。這是這一輪唯一一個「不因為信用分數而放行」的例外。

### 6. 直接做到最終版，APK 自己放上網路

**這一條 2026-08-18 當天改過一次。** 原本的計畫是先出一個只上傳、不下載規則的
建檔版，等共識可信之後再開放下行通道。使用者改成：**做到最終版**
（導航、回報、規則下發都在），**把 APK 放上網路讓有心人士下載去建檔**。

理由不只是快：**建檔的動機來自回饋迴路。** 願意在紅燈時多按一下的人，
是因為那一下會讓**他自己的**導航變準。切掉下行通道就切掉那個迴路，
建檔版對建檔者的回報只剩「謝謝，你按的東西會讓別人以後比較好用」——
那是慈善，不是產品。而這個專案需要的正是那一小群最有動機的人，不是最多的人。
Play 封閉測試同理：湊 12 個人連續 14 天，湊出來的是願意幫忙湊人數的人，
不是每天騎車經過路口的人。

**封測沒有取消，只是排在覆蓋率後面。** 順序是
**自己發 APK → 用路人建檔 → 覆蓋率夠高 → 封測 → 上架**。
排序依據是**資料成熟度，不是平台流程的前置時間** —— 這與「那 14 天是日曆時間，
越早開始越好」的直覺相反，而那個直覺假設 App 在等平台。這裡的 App 在等資料，
拿一個大多數路口都沉默的版本去跑封測，換到的是 12 個人對一個還不會講話的
導航 App 的第一印象。

**但「夠高」必須是量得出來的數字，否則「慢慢建」會變成永遠不發。**
分母不要用路口總數，用**用路人實際騎過的路口** —— 常走的路口先建到，
比冷門路口建十個有價值。而且要**同時看正確率**：
覆蓋率高而正確率低比沒有資料更糟，那會讓騎士開始相信一個會騙他的東西。
這代表**後端從第一天就要有覆蓋率儀表板**，不是等到要決定封測時才去查。

**代價要自己補，逐項在 [ADR-0015](docs/adr/0015-ship-the-full-app-and-self-distribute-the-apk.md)**：
沒有自動更新（App 內要有版本檢查）、簽章金鑰弄丟就無法更新既有安裝、
「未知來源」與 Play Protect 會勸退非技術使用者、
Play 那 14 天的時鐘不會開始跑（哪天要上架從零算起）。

**而且有一個安全性質必須換地方擺。** 建檔版原本免費提供一個保證：
未經審核的回報不可能播進別人耳朵裡，因為沒有下行通道。現在通道開了，
那個保證改由**發布閘門**提供 —— 回報照樣即時上傳、即時入庫，
但只有通過審核或達到共識門檻的規則才下發。
**也就是說審核那一套必須在第一個公開 APK 之前就完成**，不能等有人回報了再補。
原本它可以晚一步做，現在不行。

---

## 二、關於「APK 能不能時時跟資料庫回報」

**可以，技術上沒有障礙。** 但這個問題底下藏著三件必須一起決定的事，
逐項回答：

### 即時回報

Android 端就是 HTTPS POST，本機用 `observations` 排隊、`WorkManager` 重試。
**但不要做成「每筆一次連線」**：機車騎乘中網路不穩，逐筆連線在隧道與地下道
會把電池吃光。做法是累積後批次送出，或在停等時送。
騎士感覺不到差別 —— 他按下去按鈕就變了，那才是「立即」對他的意思。

後端用 Cloudflare Workers + D1 或 Supabase 的免費層。
一筆回報連同路口片段是數百位元組；100 位活躍騎士、每人每天 20 個路口，
一天約 2,000 筆、不到 1 MB。**流量不是成本風險，審核的人時才是。**

這推翻了 ADR-0008 的「每月固定成本 NT$0」，改成
「免費層內，有上限，超過就要決定收費或關閉」。離線可用、靜態發布、
山區照常騎那幾項不受影響 —— 上傳失敗只是延後。

### 前幾筆親自審核，正確率當信心指標

定案在 [ADR-0013](docs/adr/0013-backend-and-reporter-accuracy-as-confidence.md)：

1. 每個裝置一個**匿名 ID**，不綁帳號、不收廣告 ID 與 IMEI。
2. **新裝置的前 5 筆（或前 3 個不同路口，先到者為準）一律進審核佇列**，你親自看。
3. **官方那 118 條規則留著當校準基準**：有人在這些路口回報時，
   系統立刻知道他對不對，**正確率自動算得出來，不必人工看**。
   這讓需要你親自審核的量少一個數量級 —— 這也是為什麼那 118 條不該刪掉，
   即使它們不再對騎士播報。
4. 正確率過門檻的裝置，之後的回報直接計入共識（禁止左轉除外，見上）。

**正確率用 Wilson 分數下界，不用裸比例。** 第一筆答對就是 100%，
裸比例會讓一個亂按而剛好猜中的人立刻升級成可信裝置；下界會把樣本數算進去，
答對 1/1 的分數低於答對 20/20。冷啟動階段這是唯一擋得住單人刷票的東西。

**審核結果要能回頭改分數。** 你判定一筆是錯的，該裝置的正確率必須跟著降 ——
否則審核只是把錯資料擋在門口，沒有告訴系統誰在製造它。

**需要一個審核介面，不是一個 SQL 查詢。** 前幾筆全部要人看，
而看的人騎車、上班、也會累。介面難用等於資料流會塞住，
而塞住的時候使用者感覺不到，只會覺得 App 沒變聰明。

### 追蹤軌跡的告知

定案在 [ADR-0014](docs/adr/0014-upload-intersection-segments-and-say-so.md)。
上傳單位是**路口片段**：進入路口前約 10 秒到離開為止，
連同路口座標、進入方位角、離開方位角、實際走法。
單點不夠（待轉與直接左轉的差別在軌跡形狀），整趟軌跡超出所需。

**「只上傳片段」不等於「沒有軌跡」，這件事要誠實面對。**
一趟車 20 個路口就是 20 個片段，能串回去就等於完整路線。所以片段
**不帶行程 ID、不帶連續序號、批次亂序上傳、時間戳只留到分鐘**。

而且要承認一個取捨：**裝置 ID 本身就讓片段可以被串接**。
要防刷票就要認得出同一台裝置，認得出同一台裝置就等於能重建那個人的路線。
**接受它，但要在告知裡講出來**，不要用「我們只收片段」暗示做不到。

首次啟動的告知畫面要用騎士看得懂的話寫：收什麼、為什麼、不收什麼、
會不會被認出來、怎麼關怎麼刪。關閉的按鈕要在同一個畫面上按得到。

**自己發 APK 不會讓這件事變簡單，只會讓它變成完全自己的責任。**
沒有商店的表單逼你填，但台灣個資法的告知義務不因為沒有商店而消失，
而且隱私權政策要自己找地方放、自己維護版本。哪天要上架，
Google Play 的資料安全表單照同一份文字填 —— 兩份說法不一致時，
被拿來對照的是嚴格的那份，所以第一版就寫對比較省事。

**一個很實在的好消息：這個設計不需要 `ACCESS_BACKGROUND_LOCATION`。**
收集只在騎士開著 App 導航時進行，前景服務（`location` 類型）就夠。
這順帶避開 Google Play 背景位置權限那一整套審查與影片示範 ——
現在用不到，但它讓「哪天要上架」少掉一段不確定的等待。

**關掉收集的人仍然要能用導航。** 把功能扣為人質換資料會讓告知變成形式，
而且那種被迫同意在個資法下的效力本來就有疑義。

---

## 三、環境（非標準路徑，會踩雷）

工具鏈裝在 repo 之外，且**這台機器另有一套不能用的 JDK**：

| 元件 | 版本 | 路徑 |
| --- | --- | --- |
| JDK | Temurin **21**.0.12 | `C:\Users\user\Android\jdk\jdk-21.0.12+8` |
| Android SDK | platform 36 / build-tools 36 | `C:\Users\user\Android\Sdk` |
| cmdline-tools | **19.0**（舊版讀不懂現在的套件庫 XML） | 同上 |
| Gradle | 8.14.3（wrapper 自動下載） | — |
| 模擬器 AVD | `scooter`（Android 14, x86_64） | — |
| Docker Desktop | 4.86.0 | `C:\Users\user\AppData\Local\Programs\DockerDesktop` |
| Python | 3.12.9（管線零外部相依） | 系統 |

系統 PATH 上的 JDK 是 **26**，AGP 不支援。每次建置都要指定：

```bash
JAVA_HOME=/c/Users/user/Android/jdk/jdk-21.0.12+8 ANDROID_HOME=C:/Users/user/Android/Sdk ./gradlew build
```

**踩過的坑**：`adb shell` 的輸出在 Git Bash 裡會遺失，用 PowerShell 呼叫 `adb.exe`
才收得到。模擬器啟動後若沒正常關閉，`qemu-system-x86_64-headless` 會殘留並霸佔
`emulator-5554`，導致新開的模擬器連不上 —— 症狀是 `adb devices` 看得到裝置但
`getprop` 永遠沒回應。

**Python 在這台機器上印中文會變亂碼**（主控台是 cp950）。跑管線或臨時腳本時
前面加 `PYTHONIOENCODING=utf-8`，否則輸出讀不懂 —— 2026-08-18 量 OSM 覆蓋率時踩到。

### 模擬器：能開，但只有一組指令能開

**這行是可用的，實測 45 秒開機**：

```bash
emulator -avd scooter -gpu host -no-audio -no-boot-anim -accel on
```

**2026-08-16 花了大半個工作階段追一個假問題，結論記在這裡免得重蹈。** 症狀是
`adb devices` 看得到裝置但 `adb shell` 永遠不回應、qemu 的 CPU 時間完全靜止。
我依序排除了快照模式、GPU 模式、Hyper-V／WSL2 搶 WHPX（關掉 Docker 再試）、
AVD 影像損壞（另建全新 AVD）、磁碟與記憶體 —— **全部都不是**。

**真正的原因是上一段那個殘留行程，而我的清理指令抓不到它。**
`Get-Process qemu-system-x86_64,emulator,adb` **比對不到 `qemu-system-x86_64-headless`**，
那是**不同的行程名**。我用 `-no-window` 試過一次之後留下一個 headless 實例霸佔 5554，
之後每一次「乾淨的」啟動其實都在跟那個卡死的舊實例講話。

所以：

- 清理**一定**要用萬用字元：`Get-Process -Name 'qemu-system*'`，別列舉行程名。
- 關閉優先用 `adb emu kill`，再補 `Stop-Process`。
- 開之前先確認 `Get-Process -Name 'qemu-system*'` 是空的。
- adb 呼叫一律用 `Start-Job` + `Wait-Job -Timeout` 包起來，否則卡住的 `adb shell`
  會把整個工作階段拖死。

**不要動 AVD 的 `hw.gpu.enabled`。** 我一度把它從 `no` 改成 `yes` 以為那是病因，
其實原設定沒問題（`-gpu host` 走命令列指定即可），已還原，備份留在
`config.ini.bak-before-gpu-fix`。

啟動失敗時 emulator 會跳一個**強制回應的 crash 對話框**；若視窗是最小化或隱藏的，
它會看起來像「凍結在 0% CPU」。用 `-no-metrics`，或把視窗開出來看。

**滾輪要用 uinput 才測得到。** 這台 AVD 上 `adb shell getevent -pl` 只列得出
`virtio_input_multi_touch_1..11`、Power Button 與鍵盤，**沒有任何 `REL_WHEEL`**。
無論如何，`adb shell input` 沒有 scroll 命令，要送真正的 `ACTION_SCROLL`
就掛一個 uinput 虛擬滑鼠：

```bash
adb push mouse.json /data/local/tmp/ && adb shell 'uinput - < /data/local/tmp/mouse.json'
```

`mouse.json` 用 `register`（`configuration` 要 `UI_SET_EVBIT` 100 給 `EV_KEY`/`EV_REL`、
`UI_SET_RELBIT` 102 給 `REL_X`/`REL_Y`/`REL_WHEEL`）之後 `inject` `[2,8,1, 0,0,0]`，
一組就是一格滾輪。`delay` 命令要夾在中間，否則裝置註冊完就隨著 stdin 關閉被移除。

祕密放在 `pipeline/.env`（已 gitignore）：`MAPILLARY_TOKEN`、`CGU_API_KEY`。

---

## 四、App 現況

### 已完成並在模擬器上實際跑過

- 地圖（MapLibre）、頂部回報列、三橫槓選單、設定抽屜
- 定位前景服務（`location` + `mediaPlayback`）
- **警示引擎**：距離 300 m、方位角 ±30°、速度 > 15 km/h、生效時段、5 分鐘冷卻、
  依剩餘時間排序取唯一一則
- **語音**：首次啟動預合成五句話成 wav，播報時放本機檔
  （即時 TTS 首句延遲實測 **2.8–3.6 秒**）；
  `USAGE_ASSISTANCE_NAVIGATION_GUIDANCE` + `AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK`
- **語音失效警告**（`VoiceStatus` 六態，見下）
- **極簡底圖深淺兩色**、**設定落地**（DataStore）
- **固定測速警示 + 可拖曳時速圓圈**
- 種子資料庫內附於 APK，首次啟動複製到可寫位置

**79 項單元測試全綠**（core-rules 53、app 26），`./gradlew build` 通過，APK 53 MB。

### 這些現成的東西，在新範圍下要改什麼

| 現有 | 新範圍下的處置 |
| --- | --- |
| `TrackBuffer` | **✅ 已修**。方位角改由位移而非時鐘決定是否過期，另加 `isStopped()` |
| `TopReportBar` | **✅ 一半**：鎖定狀態拿掉了，改由呼叫端決定畫不畫。還缺 `A ➔ B & C` 那一行與禁止左轉按鈕 |
| `AlertEngine` | 剛性時窗從 300 公尺改成 20 秒（夾 80–350 m）；加待轉提示的 30 秒窗 |
| `AlertVoice` / `AlertPhrases` | 預合成保留給 5 秒短句，新增動態 TTS 通道給含路名的長句，兩者共用音訊焦點 |
| `SpeedDial` | 不動。它刻意不知道自己在哪個畫面上，導航畫面可以直接用 |
| `observations` 表 | 加上片段、裝置 ID、上傳狀態；`uploaded_at` 已經在了 |
| `rules` 表 | 官方那 118 條改成不播報、只當校準基準 —— 需要一個「來源」欄位或旗標 |
| `default_rules` 表 | **App 端從來沒有讀過它**（ADR-0010 就是這樣發現的）。導航回來之後要決定它到底算不算數 |

### 語音失效警告（2026-08-15，模擬器已驗證）

`VoiceStatus` 六態，因為**補救動作各不相同**，而一個只會說「語音不可用」的畫面，
是把問題丟給一個站在路邊、戴著安全帽、正要出發的人。

| 狀態 | 意義 | 給騎士的按鈕 |
| --- | --- | --- |
| `CHECKING` | 還沒有結論 | 不顯示任何東西 |
| `READY` | 五個 wav 都在 | — |
| `DEGRADED` | 引擎正常但預合成失敗，退回即時 TTS（慢約 3 秒） | 重新檢查／知道了 |
| `SILENCED` | 引擎正常但**媒體音量為 0** | 調高音量 |
| `MISSING_DATA` | 沒有 zh-TW 語音資料 | 下載語音／語音設定／重新檢查 |
| `NO_ENGINE` | 沒有可用的 TTS 引擎 | 語音設定／重新檢查 |

- **致命的三種沒有關閉鍵**。能被關掉的警告，和一開始就沒有的警告，對騎士來說沒有差別。
- **引擎問題優先於音量**。兩者同時壞掉時叫騎士去轉大聲，他轉完仍然聽不到，卻會以為修好了。
- **合成有 15 秒 watchdog**。有些引擎失敗時 `onDone` 與 `onError` 都不給；
  停在 `CHECKING` 等於這套警告機制自己也靜默失效。
- **每一輪檢查帶一個 generation 號碼**。沒有它的話，連按兩次「重新檢查」時，
  第一顆引擎（正常）的回呼可能晚於第二顆（壞掉）抵達，把狀態蓋回 `READY`。
- **導航加入後這張表要再看一次**：動態 TTS 是導航指示的唯一通道，
  `DEGRADED`（預合成失敗但引擎正常）在外掛時期只是慢三秒，
  在導航時期意味著 5 秒那句短句會遲到 —— 嚴重度變了。

### 既有的坑，仍然有效

**抽屜手勢**用 `gesturesEnabled = drawerState.isOpen`，不是 `false`。
Material3 的開啟手勢吃**整個內容區**的橫拉，騎士要把地圖往右推就會拉出設定；
寫死 `false` 會連帶把「滑掉抽屜」也拿走。

**滾輪縮放在 Compose 裡必須自己接。** MapLibre 內建那份永遠不會被呼叫 ——
`AndroidComposeView.dispatchGenericMotionEvent` 攔下 `ACTION_SCROLL` 走自己的
pointer input。所以 `MapCanvas` 有兩塊：`WheelSafeMapView` 把觸控通道裡的
`ACTION_SCROLL` 丟掉，`Modifier.wheelZoom` 在 Compose 那層做縮放。
**刻意不 consume 那些事件** —— consume 會讓 interop filter 轉進 `NotDispatching`，
下一個觸控手勢會被整個吃掉。這條路徑在真機上用不到，只影響開發手感。

**設定落地**用 DataStore。`RideViewModel.settings` 在讀回來之前是 `null`，
`ScooterApp` 那時**什麼都不畫** —— 先用預設值畫一次再換過去，
選淺色的人每次冷啟動都會看到一閃的深色，而那是最顯眼的一幀。
**服務自己訂閱設定**（`RideService.followSettings`），不等畫面來推：
`START_STICKY` 把服務拉回來時 Activity 可能根本不存在。

### 測速照相與時速圓圈（不受這次改範圍影響）

**資料**：[測速執法設置點](https://data.gov.tw/dataset/7320)（全國 1,897 筆）。
排除 292 筆國道／快速道路／高架、44 筆區間測速、38 筆座標無效，剩 **1,523 筆**進種子庫。

- **排除快速道路不能只靠關鍵字。** 地址常常只寫「台61線60.1公里處」。
  線號 `台61`～`台88` 那個區塊就是快速公路系列，**臺／台兩種寫法都要吃**。
- **「拍攝方向」有十幾種寫法**，只解析能確定**終點**的那幾種，其餘當成不限方向。
  全國有 715 筆判不出方向。不限方向的警示對所有來向發出：多播一次只是吵，
  該播沒播是騎士收到罰單。
- **殘留風險**：地下道沒有排除。復興北路車行地下道的測速點就在庫裡，
  而那條地下道實際禁行機車。沒一起排掉是因為有些地下道機車可以走，
  而 OSM 的 `motorcycle` 標籤不可信。

**時窗**：測速是**彈性**的，窗開在 500–320 公尺，比路口的剛性窗遠。
兩個窗不重疊，語音通道就不會被搶 —— 不需要任何仲裁邏輯。
**導航加入後這件事要重算**：剛性窗改成 20 秒之後，時速 60 時它是 333 公尺，
會與測速的 320 公尺重疊。**這是新範圍下第一個會回歸的既有行為。**

**播報**：一律講速限（只說「前方測速照相」等於要騎士自己回想限速多少，
而他正在騎車）。超速才加「您已超速」。測速走即時 TTS，不預合成。

**時速圓圈**：可拖曳，位置存進 DataStore。**沒有速限資料時不上色** ——
台灣沒有可靠的全路網速限（OSM 的 `maxspeed` 臺北 24.5%、台中 35%），
猜一個出來上色比不上色危險得多。

**schema 第 2 版**新增 `enforcement_sections`（**空表**）。
`ScooterDatabase.onUpgrade` 逐版套用 `Schema.MIGRATIONS`。

- **遷移是在第一次定位更新時才跑的**，因為 `ScooterDatabase` 是 lazy 的。
  也就是說遷移失敗會發生在騎乘途中，那是最糟的時機。之後要把它移到啟動時。
- **種子檔只在資料庫不存在時安裝**（`SeedInstaller`，保護未上傳的回報）。
  **自己發 APK 之後這件事更重要**：使用者是手動裝新版覆蓋舊版的，
  而尚未上傳的回報是騎士親手按的，升級把它們沖掉不能接受。

### 底圖對比（2026-08-18）

「整個都黑黑的、看不出哪裡是路」是 CARTO Dark Matter 的既有問題：
背景約 0.05 亮度、路網約 0.20–0.28，兩者都在暗部，差距在手機螢幕上又被反光吃掉。

**光柵圖磚改不了個別元素的顏色**，唯一的槓桿是 `raster-contrast` ——
它讓亮度往兩端推，背景壓成真正的黑、路網拉亮，相對差距因此放大。
深色從 **0.06 拉到 0.45**（淺色 0.02 → 0.15），另加 `raster-saturation: -0.15`
讓深色版更中性。代價是暗部細節消失，而那些細節本來就不該在騎乘畫面上。

**這是止血不是解法。** 要真正讓「路」與「背景」分開得等向量圖磚 ——
那時直接把 road 圖層指定成任何顏色，不必透過整張圖的亮度曲線去繞。
這件事與下一段的授權問題是同一個解。

### 地圖上的線圖層（2026-08-18 建好，其中一條還沒有資料）

| 圖層 | 顏色 | 狀態 |
| --- | --- | --- |
| `prohibited` | 紅 `#FF453A` | 圖層與 source 都建好，**source 目前是空的** |
| `route-casing` + `route` | 黑外框 + 琥珀 `#FFB020` | 同上，**路線引擎還沒上機** |

**顏色是設計決定，所以先訂下來，不要等到有資料那天隨手挑。**
琥珀與紅分得開，兩者與底圖的灰階也分得開 —— 一條是「照這裡走」，
一條是「別走這裡」，那兩件事在陽光下瞄一眼就要分得出來。

**✅ 禁行路段的紅線已經接上了**（2026-08-18）：
`ScooterDatabase.allProhibited()` → `RideViewModel.prohibitedLines`（IO 執行緒讀一次）
→ `MapCanvas(prohibited = …)` → `Style.setGeoJson()`。路線那條 source 仍是空的，
因為**路線引擎還沒上機**；接的時候只要餵 `MapStyle.SOURCE_ROUTE`，樣式不必動。

三個實作時的決定：

- **GeoJSON 用手寫字串，不用 MapLibre 的 `Feature`／`LineString` 型別。**
  那些型別來自 `org.maplibre.geojson`，帶進呼叫端會讓「一條折線」在專案裡有
  兩種表示法（`LatLon` 與 `Point`），而 core-rules 刻意不依賴地圖函式庫。
  轉換擋在 `MapCanvas` 這一個地方。
- **換皮之後要重餵一次。** 切換深淺色會建出一份全新的 `Style`，
  舊的 source 不會跟過來 —— 少了重餵那一步，切一次外觀紅線就消失了，
  而那看起來像「這條路不再禁行」。所以 `style` 現在是一個 state，
  資料由另一個 `LaunchedEffect` 追著它跑。
- **`allProhibited()` 一次全載，刻意不做視野範圍查詢。** 全國只有 4 筆，
  為四筆做一套隨鏡頭重查的機制不划算。**其他縣市進來時要改** ——
  那時症狀會是滑動掉幀，改成吃鏡頭範圍的 `prohibitedNear()` 即可，畫線端不動。

### ⚠ 底圖現在用的來源不能上線

CARTO（Positron／Dark Matter）**是借用的**。
[官方文件](https://docs.carto.com/faqs/carto-basemaps)載明 tile 服務僅供 enterprise
與非營利補助，免費額度 75,000 mapviews/月，**不供公開免費使用**。
改成自建向量圖磚（Planetiler，OSM／ODbL），**配色設計可以沿用**
（CARTO 的 style 是另行開放授權的），換掉的只是誰的機器在送圖。
換的時候只動 `app/.../ui/MapStyle.kt`。

**這件事與導航無關，換誰做都擋在上架前面。**

### 從未在真機上跑過

使用者只有 iPhone，正在找二手 Android。模擬器驗不了 GPS 精度、廠牌 ROM 背景清殺、
太陽下過熱。**新範圍讓這件事更急**：導航與回報全都建立在 GPS 品質上，
而「停下時的進入方位角」這種東西在模擬器上永遠是完美的。

### 測試回放

```bash
adb shell am broadcast -a tw.scooter.REPLAY -f 0x01000000 --es road 興隆路三段
```

只存在於 debug 版。從資料庫挑真實規則當目標並合成接近軌跡，會發聲。
`-f 0x01000000` 不能省。**回放器要擴充**：現在只合成「接近」，
新需求需要「接近 → 停止 → 停 90 秒 → 起步」才測得到回報流程。

---

## 五、資料現況

| 項目 | 數量 | 新範圍下的地位 |
| --- | --- | --- |
| 官方規則 | 118（待轉 21、直接左轉 95、外側專用道 1、中性 1） | **只當校準基準，不對騎士播報** |
| 手動實地查核 | 13 | **退場**（使用者決定） |
| 影像判讀 | 3 | **退場** |
| 行政區預設規則 | 24（12 區 × 2 條，只有臺北市登錄，ADR-0009） | App 端從來沒讀過，導航回來後要重新決定 |
| 科技執法點位 | 29（其中 25 個取締「不依規定轉彎」） | 照常 |
| 固定測速點位 | 1,523（全國 21 個縣市） | 照常 |
| 區間測速 | 0 寫入（`enforcement_sections` 空表；全國 44 筆原始檔已存） | 明確延後 |
| 待人工補座標 | 19 個路口 | **不再補**，等用路人騎過去建檔 |
| 種子庫大小 | 約 200 KB | 加上路網圖磚後會大很多（ADR-0003 實測全台 309 MB） |

### 手動建檔退場，具體要做什麼

使用者決定放棄自己建檔（`field_checks.json` 裡的 13 筆實地查核與 3 筆影像判讀），
全部交給用路人。**但這件事目前只改了文件，資料與程式都還沒動。**

`field_checks.json` **不要直接刪掉**。它進了 git，刪掉只是讓它變得難找，
而那 13 筆是有人真的騎過去看過牌子的事實 —— 哪天要拿來驗證群眾資料的準確度，
它就是唯一的獨立對照組。要做的是**讓管線不再讀它**，並在檔案開頭註明退場日期與理由。

官方那 118 條要在 schema 上區分出來（來源欄位或旗標），
因為它們的用途變了：不再是顯示規則，而是**評分回報者的答案卷**。
`build_seed.py` 與 `Schema.kt` 兩邊都要動，而兩邊的建表語句本來就必須一致。

管線見 [pipeline/README.md](pipeline/README.md)。執行順序：

```bash
python taiwan.py && python default_rules.py   # selftest，改過那兩個模組才需要
python taipei_parse.py && python taipei_geocode.py && python import_enforcement.py && python build_seed.py
cp build/scooter_seed.db ../app/src/main/assets/
```

**管線沒有測試框架**（零外部相依），純函式一律用 `--selftest` 自我驗證。
改動會影響所有規則的地方時，**先跑一次 `build_seed.py` 把數字存成基準再動手**。

---

## 六、路線引擎：選定 BRouter，Valhalla 的 Android 那一層不存在

**2026-08-18 去確認 ADR-0003 的前提，結果是它站不住。**

Valhalla 的 README 只說它 "is also used on iOS and Android devices"，
overview 說 C++ 架構 "should allow for cross compilation" —— 兩句都是可能性，
不是產品。**沒有官方 AAR、沒有 NDK build target、沒有 Android binding**；
Node.js 與 Python bindings 都有文件，Android 沒有。搜到的 `mapzen/on-the-road`
是 Valhalla **服務**的 HTTP 客戶端，而 Mapzen 2018 就收攤了。

所以 ADR-0003 那條路的第一步，是一段沒人維護、沒有文件、而我們**連真機都沒有**
可以驗證的 NDK 交叉編譯工作。那不是工程量大，是風險未知。

### 替代方案：BRouter（[ADR-0016](docs/adr/0016-brouter-instead-of-valhalla-on-device.md)）

GraphHopper 官方在放棄 Android 離線支援時，指向的就是 BRouter。量出來的對照：

| | Valhalla | BRouter |
| --- | --- | --- |
| 台灣圖資 | **309 MB** | **約 33 MB**（`E120_N20.rd5` 21.0 MB + `E120_N25.rd5` 11.9 MB） |
| Android | 無官方 binding，自己用 NDK 建 | **純 Java**，官方自己就有 Android App |
| 維護 | 引擎活躍，不含 Android | 圖磚**每週重建**（實測 last-modified 就是查詢當天） |
| 轉向限制 | 原生 | 1.4.8（2016）起支援，`considerTurnRestrictions` |
| 轉向成本 | 有 | `turncost`、`initialcost` —— 正好是 ADR-0012 的待轉成本要用的 |

**33 MB 對 309 MB 不只是省流量**：ADR-0008 那一整套分縣市離線包與首次啟動
下載流程，是為了 309 MB 而設計的；33 MB 是「隨 APK 出貨」的量級。

### Spike 已經跑過了，通過（2026-08-18）

**不裝 BRouter App、不走它的 service interface，用一般的 `java -cp` 就算得出路線。**

`brouter-1.7.10-ro.jar` 是 **346 KB、128 個 class、零個 android 參照**的純 Java
路由核心（release zip 裡另有一個 2.3 MB 的 `-all.jar`，那個是含建圖工具的）。
餵它一份 `E120_N25.rd5`，台北車站 → 市府：**5,714 公尺、279 毫秒、157 節點、
6 則轉向指示**。台北 → 台中（moped profile）：156.6 公里、1.8 秒、58 則指示。

重跑的方法（整個 spike 不到 70 行）：

```java
RoutingContext rc = new RoutingContext();
rc.localFunction = "…/profiles2/moped.brf";  // lookups.dat 要放得到
rc.turnInstructionMode = 1;
RoutingEngine engine = new RoutingEngine(null, null, new File("segments4"), waypoints, rc);
engine.doRun(0);
OsmTrack track = engine.getFoundTrack();   // track.distance / track.nodes / getVoiceHint(i)
```

座標是整數微度加偏移：`ilon = (int)((lon + 180) * 1e6 + 0.5)`、
`ilat = (int)((lat + 90) * 1e6 + 0.5)`。圖磚放在 `segments4/`，
從 `https://brouter.de/brouter/segments4/<名稱>.rd5` 下載。

### Spike 查出來的三件事，第一件是唯一真正的整合成本

**一、轉向指示的欄位是 package-private。** `VoiceHint` 的 `cmd`、`angle`、
`distanceToNext` 都不公開，公開的只有 `getTime()`、`getExitNumber()`、
`formatGeometry()`、`hasGiveWay()`。官方預期你走 GPX／GeoJSON 序列化那條路。
我們要逐向指示，所以得選一個：**把 adapter 放進 `btools.router` 套件**
（Java 的老招，能用但難看）、序列化再解析回來（更難看）、或者 fork。
**建議走第一個**，並且把那個檔案獨立放好、註明理由，否則下一個人會以為
放錯地方了。

**二、內建的 `moped.brf` 不禁止走高速公路。** 成本表裡是
`if (highway=motorway) then 30` —— 那是「貴」不是「不能走」。
實測台北到台中，moped 156.6 公里、car-fast 166.9 公里，**兩者分不出誰上了國道**，
所以這個測試本身不決定性，而結論是**不能沿用內建 profile**。
白牌 profile 要自己寫，`car-fast.brf` 裡的 `assign avoid_motorways` 布林參數
就是可以照抄的寫法。這是工作量不是風險。

**三、`turncost` 與節點的 `initialcost` 都在 profile 裡。**
`moped.brf` 第一行就是 `assign turncost = if junction=roundabout then 0 …`，
節點區段另有一個 `initialcost`。**這正是 ADR-0012 要的** ——
待轉要表達成「這個轉彎多花一個號誌週期」，在 BRouter 上是改設定不是改引擎。

### 白牌 profile 寫好了，而且國道的禁行是實測驗證過的

`app/src/main/assets/brouter/scooter-tw.brf`，**由 `car-fast.brf` 改成，不是由
`moped.brf`** —— 內建的 moped profile 自己在檔頭寫著 "DO NOT USE FOR ACTUAL
NAVIGATION / Turn restrictions are missing"，而 car-fast 是維護中的完整 profile。

白牌與汽車真正影響路線的差別只有一件事：**禁行的路段不同**。速度模型、
轉向成本、城鎮懲罰沿用汽車的就好 —— 機車在台灣市區的平均速度與汽車接近，
差別在紅燈不在路型。

硬禁三類（`costfactor 10000` 是 BRouter 的「等同不能走」慣例）：

| 判準 | 為什麼是它 |
| --- | --- |
| `highway=motorway` 或 `motorway_link` | 國道 |
| `motorroad=yes` | 快速道路的標準標法。**不能用 `highway=trunk` 判** —— 臺北市 trunk 有 322 條而 motorroad=yes 只有 83 條，trunk 包含大量一般幹道 |
| `motorcycle=no` | 有標的就信（臺北 201 條）。反過來的 `motorcycle=yes` **不可信**（復北地下道實際禁行卻標 yes），所以只拿它當禁止的依據，不當許可的依據 |

**刻意不禁橋、不禁高架、不禁 `layer>0`。** 有些高架機車可以走，而橋更是非走不可 ——
**臺北市光是有 `bridge` 標籤的路就 3,370 條、`layer>=1` 有 2,903 條**，
禁掉等於淡水河上一條都過不去。查不到的交給用路人回報。

那 5 段官方公告的全面禁行不在 profile 裡處理：它們是 way 編號的清單，
走 ADR-0006 的事後驗證。

#### 驗證：台北車站 → 台中車站

不是比距離，是拿路線的座標去問 OSM。每隔幾十個節點抽一點，查半徑 20 公尺內
有沒有國道／快速道路：

| | 距離 | 轉向指示 | 20 m 內命中國道 | 命中 `motorroad=yes` |
| --- | --- | --- | --- | --- |
| `car-fast`（對照組） | 166.9 km | 25 | **110 條** | 13 條 |
| `scooter-tw` | 184.9 km | 86 | **0 條** | 11 條 |

**國道 110 → 0，禁行成立。** 對照組會上國道，我們的不會。

**但快速道路那 11 條不能當成失敗，也不能當成成功 —— 這個測法在這裡失效。**
台灣的快速道路常常直接架在平面道路正上方，20 公尺的水平半徑分不出
「走在上面」與「走在下面」。這與禁行路段比對遇到的是同一個垂直分離問題。
**而 way 編號這條路已經證實走不通** —— BRouter 不帶 OSM way 編號（見下）。
所以嚴謹驗證只能靠幾何加上垂直資訊，而 OSM 的 `layer` 我們也沒在用。
**實務上不必再驗**：profile 對 `motorroad=yes` 是 `costfactor 10000`，
那是結構上的硬禁，不是統計上的傾向 —— 國道 110 → 0 已經證明這個機制有效。

#### 轉向限制：實測有效

不依賴「car profile 預設就開」這個沒寫在文件裡的行為，profile 裡明寫
`assign considerTurnRestrictions = true`。驗證方式是拿同一份 profile 複製兩份、
只差這一行，跑台北市內帶 `no_left_turn` 關聯的路口：**rel 3940135 關閉 112 公尺、
開啟 118 公尺** —— 同一組起訖走了不同的路。

（用 `car-fast` 直接測會看不出差別，因為對 car profile 它預設就是開的；
測了 8 組全部相同。要看出差別必須用預設關閉的 bike profile 當底。）

### ⚠ 用路人回報的禁止左轉，目前到不了路線引擎

這是這一輪最重要的發現，而且它影響需求第 5 條。

**BRouter 的 profile 讀的是 OSM 標籤，讀不到我們的資料庫。** 所以：

- OSM 已有的那 1,204 條轉向限制 → ✅ 有效。
- **騎士回報的禁止左轉 → ❌ 進不了路線計算**，只能拿來播報。
- 待轉成本（ADR-0012）→ ❌ 同樣進不去。`turncost` 是按 OSM 標籤算的，
  它不知道哪個路口要待轉。

三條路可以走：

1. **自建 rd5 圖磚**，在建圖時把我們的規則注入成自訂標籤
   （`-all.jar` 就是建圖工具，而我們本來就有一條管線在產種子庫）。
   這是唯一能讓回報真正「讓導航變聰明」的路。
2. **算完再驗證**：路線算好後比對我們的禁止左轉表，違反就加懲罰重算。
   BRouter 有 `nogo` 點，但那是圓形，會把整個路口封掉而不是只封一個轉向。
3. **只播報不繞路**：待轉走這條是可以接受的（ADR-0012 說它只是慢一個週期，
   到得了）；**禁止左轉走這條不行**，那是合法性問題。

**建議走 1。** 但要知道那是一條新的管線分支，不是一個設定。
在它做出來以前，回報對導航的貢獻只有播報，第 5 條的「越來越聰明」打折。

### 引擎已經接進 App（2026-08-18）

| 檔案 | 做什麼 |
| --- | --- |
| `app/libs/brouter-1.7.10-ro.jar` | 路由核心。**沒有 Maven 座標**，只在 GitHub release 的 zip 裡，所以版本寫在檔名、升級是手動 |
| `app/src/main/assets/brouter/scooter-tw.brf` | 白牌 profile |
| `app/src/main/assets/brouter/lookups.dat` | 標籤字典，**必須與 jar 同版本** |
| `route/BrouterFiles.kt` | 把上面兩個複製到可寫位置（BRouter 的 API 吃檔案路徑，assets 不是路徑） |
| `route/SegmentDownloader.kt` | 下載兩塊圖磚，支援 Range 續傳 |
| `route/ScooterRouter.kt` | 算路線，回傳幾何與距離 |

目錄結構照 BRouter 自己的慣例（`profiles2/`、`segments4/`），不是自己發明的 ——
哪天要拿 BRouter App 的圖磚對照、或照它的文件除錯，路徑一樣才對得起來。

**圖磚不隨 APK 出貨。** 33 MB 塞得進去，但那些圖磚是**每週重建**的，
而我們自己發 APK 沒有自動更新（ADR-0015）—— 塞進去等於把路網更新綁在
「使用者想到要下載新版 App」上面。分開下載也讓規則與路網走不同通道。

**兩塊都要下載。** 台北在 25.03°N，剛好落在 `E120_N25` 與 `E120_N20` 的
分界北邊，只抓一塊會讓台北往南算不出路線 —— 症狀是「有些目的地就是找不到路」，
看起來像資料壞掉，不像少了一個檔。

**`ScooterRouter` 刻意不回傳轉向指示**，因為 `VoiceHint` 的欄位是
package-private，現在讀不到內容。不假裝有那個資訊比回傳一個空清單好。

**release 目前沒有開 minify。** 哪天要開，BRouter 的 expression 類別需要
keep 規則 —— profile 是在執行期解析的，被混淆掉的症狀會是「路線算不出來」
而不是編譯錯誤。

### 英文版與日文版：現在做不出來，卡的是資料不是翻譯

使用者問要不要現在做多語版（含語音）。**量了才回答**，臺北市有名字的道路：

| | 筆數 | 覆蓋率 |
| --- | --- | --- |
| `name`（中文） | 25,534 | — |
| `name:en` | 12,805 | **50.1%** |
| `name:ja` | 156 | **0.6%** |

**英文版有一半的路唸不出英文名，日文版等於零。** 而且真正的難點不在介面翻譯 ——
介面字串本來就都在 `strings.xml`，加一個 `values-en/` 是一天的事。難的是**語音**：
導航指示長這樣「前方 300 公尺，忠孝東路左轉」，路名是中文而 en-US 的 TTS 引擎
唸中文是噪音。混語言 TTS 不是設定問題，是沒有好解法的問題。

所以**先把 App 做完**。這不是排優先序，是那件事現在做不出來。

真的要服務外國騎士，比翻譯有效得多的兩件事：**介面改用圖示與數字**
（待轉／直接左轉本來就適合畫成圖），以及**語音只講「左轉」「待轉」不講路名** ——
那是設計選擇，不需要任何翻譯工程，而且對本地騎士也不會變差。

唯一現在值得注意的 retrofit 成本是 `AlertPhrases.kt` ——
語音句子是硬編碼在 Kotlin 裡而不是 `strings.xml`。但那只有幾句話，
之後再搬不會比現在搬貴多少，所以連這個都不急著動。

### 還沒解決的

**`-ro.jar` 沒有 Maven 座標**，只在 GitHub release 的 zip 裡。所以要嘛放進
`app/libs/`（升級變手動作業），要嘛從原始碼建。哪一種都要在 build 檔裡寫死版本，
否則哪天沒人知道那個 jar 是哪來的。

**圖磚要出貨或下載。** 台灣兩塊共約 33 MB（`E120_N20.rd5` 21.0 +
`E120_N25.rd5` 11.9）。33 MB 可以直接塞進 APK，也可以首次啟動抓 ——
但它們是**每週重建**的，所以塞進 APK 等於資料更新綁在 App 更新上，
而我們是自己發 APK（ADR-0015）沒有自動更新。**建議首次啟動下載**，
與規則同步走不同通道。

**BRouter 的 voicehints 夠不夠當逐向指示用**，仍然沒有驗證 —— 數量對得上
（台北到台中 58 則），但內容品質要接出來看過才知道，而那卡在上面第一件事。

---

## 七、已驗證的假設

| 假設 | 結果 | 量的日期 |
| --- | --- | --- |
| 台灣 Valhalla 圖磚體積 | **309 MB**（層級 2 佔 82%），建置 6 分 14 秒 | 08-15 |
| `motor_scooter` 會避開國道 | ✅ 台北→台中：汽車走中山高，機車 167.6 km 零國道 | 08-15 |
| Mapillary 台北覆蓋率 | 87%（93/107 路口） | 08-15 |
| Python 與 Kotlin 的網格索引一致 | ✅ `GridParityTest` 把關 | — |
| 即時 TTS 首句延遲 | **2.8–3.6 秒**（這是 5 秒短句必須預合成的原因） | 08-15 |
| **臺北市 OSM 轉向限制數量** | **1,204 條關聯**（`no_left_turn` 600 條為最大宗） | **08-18** |
| **臺北市車道級禁行機車** | **1.1%**（`motorcycle:lanes`，3,550 段主要幹道） | **08-18** |

### 2026-08-18 量的那兩筆，細節

臺北市 `primary|secondary|trunk` 共 3,550 段，與 ADR-0009 在台中量的 1,191 段對照：

| 標籤 | 臺北 | 台中 | 用途 |
| --- | --- | --- | --- |
| `oneway` | 97.1% | 98% | 單行道 |
| `lanes` | 68.0% | 39% | 車道總數 |
| `maxspeed` | 24.5% | 35% | 速限 |
| `motorcycle` | 5.6% | 9% | 路段級禁行機車（有標也不可信） |
| `lanes:forward` | 2.4% | 6% | 單向車道數 |
| `turn:lanes` | 1.0% | 1% | 哪條車道往哪轉 |
| **`motorcycle:lanes`** | **1.1%** | **0%** | **車道級禁行機車** |

兩個城市的結論一致：**車道級禁行機車嚴格說不是零，實務上不能用。**
臺北的 `lanes` 覆蓋率比台中好很多（68% vs 39%），所以這不是「臺北資料比較好」
可以解決的問題 —— 是這一類標籤在台灣根本沒有人在標。

轉向限制則相反，**是有資料的**：1,204 條關聯對上 4,794 個號誌節點。
這個比值**不能當覆蓋率**（分母應該是「現場真的立了禁轉牌的路口」，
而那個數字沒人知道），但量級與問題規模相稱，而且 Valhalla 原生就吃它。

重跑這兩個量測的腳本沒有進 repo（一次性的 Overpass 查詢）。
要重跑的話，查的是 `relation(area)["type"~"^restriction"]` 與
`way(bbox)["highway"~"^(primary|secondary|trunk)$"]`。
**用 bbox 不要用 area 查 way** —— 2026-08-18 用 `way(area.a)["motorcycle"]`
回傳 0 筆，換成 bbox 同樣範圍回傳 774 筆。那個 0 看起來像事實，不像故障。

---

## 八、關鍵領域知識（血淚換來的）

**方向的表述**：本專案一律用**面向**（車頭朝哪）。政府開放資料的「方向」欄用的是
**來向**（「北往東」= 從北邊過來），兩者第一個字恆為相反。匯入時就轉換，
不讓兩種說法並存。

**規則掛在進入方位角上**，不是路口。同一個路口不同來向規定可能不同 ——
市府路×松壽路面向北免待轉、面向南要待轉。

**路口兩側路名常常不同**：興隆路三段路口東側是萬芳路、西側是興隆路三段192巷8弄；
成功路四段路口北側金湖路、南側金龍路。官方 CSV 只給一個名字，雙向規則展開時
兩列會繼承同一組路名，其中一半是錯的。
**這正是回報介面要用 `A & B` 並列的原因** —— 不是為了好看，是因為單一路名本來就常常錯。

**標誌落差**：官方公告與現場標誌換設有時間差，兩者衝突時**一律播待轉**
（待轉在任何情況下都不違法）。成功路四段就是官方寫免待轉、待轉牌還立著。

**待轉區方框不是判定依據**：核准免待轉後方框不會鏟掉，而且大路口只要任一側需要待轉，
畫面裡就看得到。

**OSM 的 `motorcycle` 標籤不可信**：復北地下道實際禁行機車，OSM 標成 `motorcycle=yes`。
臺北量到 187 條標 `motorcycle=no`、291 條標 `yes`、147 條標 `designated`，
而已知至少有一條是錯的。改用結構性判定（`tunnel`／`bridge`／`layer` 加名稱關鍵字）。

**路名是「臺」不是「台」**：查 `台灣大道` 只回一段，OSM 用的是 **`臺灣大道`**。
台北的資料剛好沒踩到，台中會整批對不上。路名比對**必須先正規化臺/台**。

**兩段式左轉標誌有兩種**：圓形藍底畫機車配左箭頭與直行箭頭（那兩個箭頭是同一動作
的兩階段，不是「可左轉也可直行」—— 判讀錯過一次，錯在危險方向）；
直式長方形紅底白字寫「前方路口機車兩段左轉」。

**街景自動判讀的結論是不要用。** 完整實作與兩輪修正之後一致率只有 **29%**，
放寬成只看「要不要待轉」的二分也只有 43%，且出現**危險方向的錯誤**
（官方要待轉、判讀說不用）。天花板在資訊本身：現場常同時存在左轉專用道箭頭與
待轉標誌，照片分不出那條車道是給汽車還是機車。加大模型或改提示詞都不會解決。
Google Street View 另外還有條款問題：禁止轉成衍生資料集，而這個專案要公開發布。

---

## 九、路線圖

**目標改成一次做到最終版再發布**（ADR-0015），所以下面**每一項都在第一個公開
APK 之前**，沒有中途出貨的縮減版。

| # | 項目 | 內容 |
| --- | --- | --- |
| ~~**0**~~ | ~~**修既有缺陷**~~ **✅ 2026-08-18 完成** | `TrackBuffer` 方位角、停止判定、遷移不在騎乘中跑、速限不再被警示門檻擋掉。見第二節的表 |
| **1** | **回報介面其餘部分** | `A ➔ B & C` 那一行；禁止左轉按鈕（兩條離開路時裂成兩顆）；每筆帶**路口節點**座標而非騎士位置。**全部要等路網圖上機** |
| **2** | **後端與上傳** | 匿名裝置 ID、批次上傳、`observations` 加片段欄位。免費層起步（ADR-0013）。**覆蓋率儀表板一起做** —— 沒有它就沒有東西決定何時封測 |
| **3** | **審核、正確率與發布閘門** | 前 5 筆進佇列；官方 118 條自動評分；Wilson 下界；審核結果回頭改分數。**閘門這一段不能延後** —— 下行通道一開，未審核的資料就會播進別人耳朵裡 |
| **4** | **告知畫面與隱私權政策** | 首次啟動的告知、關閉與刪除。**不需要背景位置權限**（ADR-0014）。政策要自己找地方放 |
| **5** | **路線引擎** | **BRouter**（ADR-0016，嵌入 spike 已通過）。要做的是：`-ro.jar` 進 build、圖磚下載、**自己寫白牌 profile**（內建 moped 不禁國道）、轉向指示的 adapter（欄位是 package-private）、禁行路段事後驗證 |
| **6** | **轉向指示與 20 秒時窗** | 剛性窗改時間；待轉提示 30 秒；動態 TTS 與預合成兩條路共存。**測速的 500–320 m 窗會與新的剛性窗重疊，要一起重算** |
| **7** | **待轉成本接進路線** | ADR-0012。**不要編成 `no_left_turn`** |
| **8** | **地址搜尋與偏航重算** | ADR-0007 的 Consequences 那幾項 |
| **9** | **「這裡沒有資料」的畫面** | 第一版覆蓋率會很低。沒建檔的路口要看得出來是**我們不知道**，不是**這裡沒有規則** —— 兩者對騎士的意義完全相反 |
| **10** | **換掉底圖** | CARTO 不供公開免費使用。**自己發 APK 不能規避這一條** —— 公開免費發布正好就是它禁止的用法。改自建向量圖磚（Planetiler） |
| **11** | **真機驗證** | 從未在真機上跑過。GPS 精度、ROM 背景清殺、過熱。**發布前的最後一道，不能省** |
| **12** | **發布** | 下載頁（要把「未知來源」每一步畫出來）、簽章金鑰離線備份、App 內版本檢查 |

發布之後才進入下一段，而下一段的節奏由資料決定不由程式決定：

| 階段 | 條件 |
| --- | --- |
| **收資料** | 用路人騎、回報、審核。**後端要有覆蓋率儀表板**（第 2 項一起做，不要事後補） |
| **封測** | 等**經過次數加權的已建檔比例**與**正確率**同時到門檻才開始。12 位測試者連續 14 天 |
| **上架** | 封測完成 + 底圖換掉 + 隱私權政策就位 |

**第 10 項最容易被當成上架才要處理的事，那是誤會。** CARTO 的條款管的是使用方式
不是發布管道，把 APK 放上網路讓任何人下載，就是它寫明不供應的那種用法。

### 明確延後（不是忘記）

1. **區間測速** —— 使用者明說**上線前一定要加回去**。schema 位子已留好
   （`enforcement_sections`，空表），全國 44 筆原始資料存在
   `pipeline/build/enforcement_sections_raw.json`。
   它是**狀態**不是接近事件，硬塞進點模型只會在起點響一次然後沉默。
   **原始資料每筆只有一組座標，但地址裡寫著頭尾里程**
   （例：`台9線68k+600m至56k+600m`），第二個端點要從里程推 —— 那是實作時的第一道題。
2. **被動觀察的演算法** —— ADR-0005 的軌跡特徵（穿越 → 停止 → 停留 → 原地轉 90 度 → 起步）
   **一行都還沒寫**，也從未以真實軌跡驗證。它是本專案風險最高的假設，
   而路口片段（ADR-0014）正是為它上傳的。主動回報先出貨，被動觀察後補。
3. **群眾影像判讀的信任模型** —— 回報者有 GPS 證明他在現場，影像判讀者在家裡，
   沒有東西可驗證。ADR-0014 之後這題更遠了：規模化押在有 GPS 物理證據的東西上。
4. 監視器影片行為分析、即時路況與 ETA。
5. 懸浮視窗 —— ADR-0011 之後降級。自己就是導航 App，警示畫自己的畫面；
   懸浮視窗留給「切到別的 App 時仍要警示」。
6. **封測與上架 Google Play** —— **明確排在建檔覆蓋率之後**，不是放棄。
   那 12 位測試者連續 14 天的時鐘要等覆蓋率與正確率同時到門檻才開始跑。
   延後的量是四週日曆時間，知道就好，不要拿它當理由去插隊。

### 賺錢

神盾模式：廣告綁在**手動更新資料庫**這個動作上，騎乘中永遠不出廣告。
第一版全部免費（護城河是資料，先收錢會同時擋住用戶和資料）。
自己發 APK 的階段更不該放廣告 —— 來的是自己找上門的有心人士，
向他們收第一筆錢會換掉整批人。

---

## 十、協作方式的備註

- 溝通用**繁體中文**，API 名稱保留英文
- **資料一律由用路人建檔**。不要再提議做手動建檔工具，也不要催使用者去判讀街景 ——
  那條路 2026-08-18 已經明確放棄
- **追蹤軌跡這件事必須明確告知使用者**，這是使用者的要求，不是合規建議
- 使用者會貼街景截圖，可直接判讀
- **不要用程式去修補程式**：用 Python 腳本改 Python 檔踩過兩次坑
  （`\n` 變成真換行、Kotlin 的 `?.` 寫進 Python）。要改就用編輯工具，
  改完先 `ast.parse` 再跑
- 改既有迴圈時注意**變數殘留**：把賦值移進 `if` 裡會讓上一圈的值留下來，
  曾讓禁行判定整批失效
