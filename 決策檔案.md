# 決策檔案

**這裡放的是「需要你拍板、我先跳過的問題」。** 每一條都寫清楚：卡在哪、
我暫時怎麼處理、選項各自的代價。看完一次回答完，我照著做。

已經拍板的長期決策在 [docs/adr/](docs/adr/)，這裡只放**還沒定案的**。
一條定案之後就從這裡刪掉，寫進 ADR 或直接實作。

日期：2026-08-18

---

## D1 — 目的地怎麼輸入

**卡在哪**：導航要有目的地，而我們沒有搜尋。POI 只有 OSM 的量，
搜尋品質贏不了 Google（ADR-0008 早就寫了這一條）。

**我暫時怎麼做**：先做**地圖上長按選點**與**座標貼上**。
兩者都不需要任何搜尋後端，而且長按選點對「我知道大概在哪」的情境夠用。

**選項**：

| | 代價 |
| --- | --- |
| A. 只做長按選點（現在這樣） | 不知道地址的目的地找不到。對通勤族影響小，對「去一個沒去過的店」影響大 |
| B. 接 Google Maps 分享連結 | ADR-0008 已經允許：使用者主動分享的連結可以跟隨重導向取座標，但絕不查 Google 的搜尋端點。**體驗其實不差** —— 台灣人本來就習慣在 LINE 裡傳 Google Maps 連結。要多寫一個連結解析與剪貼簿監看 |
| C. 自建 OSM 地名搜尋 | 離線可用、零成本，但台灣 OSM 的 POI 稀疏，搜「全家」會漏掉九成。做出來會被當成壞掉 |

**我的建議是 A + B。** B 的成本低而且它剛好貼合台灣人的使用習慣。

---

## D2 — 導航中要不要跟著騎士轉動地圖

**卡在哪**：導航畫面通常會把地圖轉成「車頭朝上」，但 `MapCanvas` 目前
**刻意關掉了旋轉手勢**（`isRotateGesturesEnabled = false`），註解寫的是
「騎乘中不需要轉動地圖，關掉可避免誤觸把方向轉歪」。

那條註解在外掛時期是對的。導航時就不一定了 —— 車頭朝上對「下一個路口要往哪」
的判讀快得多，尤其在複雜路口。

**我暫時怎麼做**：維持北方朝上，因為那是現在的行為，改了要真機驗證才知道好壞。

**要決定的是**：導航中要不要自動轉成車頭朝上（使用者仍然不能手動轉）。
代價是暈眩感因人而異，而且我們沒有真機可以試。

---

## D3 — 算不出路線時要說什麼

**卡在哪**：`ScooterRouter.route()` 回傳 null 有三種原因：圖磚還沒下載、
起訖點附近沒有機車走得的路、兩點在白牌路權下真的不連通。
**三者對騎士的意義完全不同**，但 BRouter 的錯誤訊息分不出來。

**我暫時怎麼做**：一律顯示「算不出路線」，並在 log 裡記原因。

**要決定的是**：第三種情況（真的不連通）值不值得特別處理。
它在台灣會發生 —— 例如目的地在只有快速道路能到的地方。
那時最有用的回答不是「算不出路線」，而是「白牌到不了這裡」，
但要分辨得出來，得用汽車 profile 再算一次比對，也就是每次失敗都多算一次。

---

## D4 — 待轉與騎士回報的禁止左轉，要不要自建圖磚

**卡在哪**：這是目前**唯一會讓需求第 5 條打折**的事。
BRouter 的 profile 讀 OSM 標籤，讀不到我們的資料庫，所以：

- OSM 已有的 1,204 條轉向限制 → 有效
- **騎士回報的禁止左轉 → 進不了路線計算**
- 待轉成本（ADR-0012）→ 同樣進不去

**我暫時怎麼做**：兩者都只播報不繞路。待轉這樣是可以接受的
（ADR-0012：它只是慢一個號誌週期，到得了）；**禁止左轉這樣不行**，
那是合法性問題，只是現在沒有別的辦法。

**選項**：

| | 代價 |
| --- | --- |
| A. 自建 rd5 圖磚，建圖時注入我們的規則 | 唯一能讓回報真正改變路線的路。`-all.jar` 就是建圖工具，而我們本來就有一條管線。但那是一條新的管線分支，而且圖磚要自己 host、自己每季重建 |
| B. 算完再驗證，違反就加懲罰重算 | 不必自建圖磚。BRouter 有 `nogo` 點但那是**圓形**，會把整個路口封掉而不是只封一個轉向 —— 會產生比原本更糟的繞路 |
| C. 維持只播報 | 零成本。代價是「回報讓導航變聰明」這句話只對待轉成立，對禁止左轉不成立 |

**我的建議是先 C 後 A。** A 值得做，但它應該排在有足夠回報量之後 ——
沒有資料的時候自建圖磚只是把 OSM 重打一次包。

---

## D5 — 轉向指示要不要講路名（**這條會改變語音架構**）

**卡在哪**：BRouter **不提供路名**。它的 rd5 只存路由用得到的標籤，
實測轉向指示帶回來的是 `highway=secondary surface=asphalt oneway=yes` 這種東西，
沒有 `name`。它是路由引擎不是圖資供應商。

原本規劃的播報是「前方 300 公尺，**忠孝東路**左轉」。沒有路名就講不出來。

**我暫時怎麼做**：播報不含路名 —— 「前方 300 公尺左轉」。

**而這件事有一個很大的附帶好處，值得單獨拿出來看**：

沒有路名，**所有播報句子就是一個有限集合**（左轉／右轉／待轉／各種距離級距）。
有限集合可以**全部預先合成**，於是：

- 即時 TTS 首句 2.8–3.6 秒的延遲**整個消失**
- 「5 秒前的確認短句」那個硬期限問題不存在了
- `VoiceStatus` 的 `DEGRADED`（預合成失敗退回即時 TTS）嚴重度下降
- 多語版的難點也跟著變了 —— 難的本來就是「用英文引擎唸中文路名」

**選項**：

| | 代價 |
| --- | --- |
| A. 不講路名（現在這樣） | 「前方 300 公尺左轉」。在單純路段夠用，**在連續路口或岔路會不夠** —— 騎士不確定是哪一個路口。可以用地圖畫面補 |
| B. 自建路名索引進種子庫 | 我們的管線本來就在抓 OSM 路網幾何（`taipei_geocode.py` 就是幹這個的），把「座標 → 路名」建成一張表不是新工程。代價是種子庫變大、要維護，而且**語音又變回動態的**，2.8–3.6 秒延遲跟著回來 |
| C. 只在關鍵處講路名 | 例如只有「待轉」那一則講路名。折衷，但兩套語音路徑都要留著 |

**我的建議是 A，而且不要急著往 B 走。** 先出貨、看騎士實際上會不會迷路。
如果真的需要，B 隨時做得起來，而且那時我們已經知道是哪些路口需要。

---

## D6 — `prohibited_segments.way_ids` 現在沒有用途

**背景**：我先前說「way 編號是給路線引擎事後驗證用的」（ADR-0006）。
**那句話錯了。BRouter 完全不帶 OSM way 編號** —— rd5 是重新編碼過的圖，
查遍 jar 裡沒有 `wayId` 之類的東西。

所以 ADR-0006 的事後驗證**只能用幾何**（點到折線距離），
而那正好就是 `ProhibitedMatcher` 已經在做的事 —— 那段程式的做法是對的。

**要決定的是**：`way_ids` 這個欄位留不留。

- **留**：如果哪天走 D4 的 A（自建 rd5 圖磚），way 編號就是注入自訂標籤時的鍵。
- **刪**：現在沒有任何東西讀它，留著會讓人以為有用途。

**我的建議是留，但把註解改成「給未來自建圖磚用」**，不要再寫成事後驗證的鍵。
（HANDOVER 與 `Schema.kt` 的註解我已經照這個更正了。）

---

## D7 — 回報列上的「XX路 ➔ YY路」拿不到路名

**卡在哪**：需求第 3 條要回報按鈕上方寫「什麼路左轉到什麼路」，
左轉可達兩條路時用 `&` 並列。**但路名沒有來源** ——
BRouter 不給路名（D5），而我們也沒有其他的路網名稱索引。

連帶影響：**「有兩條離開路時把禁止左轉按鈕裂成兩顆」也做不到**，
因為我們不知道有幾條離開路、更不知道它們叫什麼。

**我暫時怎麼做**：禁止左轉是**單獨一顆按鈕**，紅底、自己一列 ——
與上面四顆分開，因為它回答的是「能不能轉」不是「怎麼轉」。
按鈕上方那一行維持現況（有官方路名時顯示，沒有就空著）。

**這條與 D5 是同一個根**：解了 D5 的 B（自建路名索引），D7 自動就解了。
反過來如果 D5 選 A（不講路名），D7 就永遠停在現在這樣。

**要注意的代價**：兩條離開路的路口，騎士按下「禁止左轉」時我們不知道
他指的是哪一條。這種回報進了資料庫會是模糊的。
在自建路名索引出來以前，這類回報**應該一律進審核佇列由人看**——
反正 ADR-0013 已經規定禁止左轉不論正確率都要審核，所以這一點不必額外處理。
