# 決策檔案

**這裡放的是「需要你拍板、我先跳過的問題」。** 定案的搬到上半部留紀錄，
還沒定案的放下半部。長期的架構決策在 [docs/adr/](docs/adr/)。

日期：2026-08-18

---

# 已定案（2026-08-18）

## D1 — 目的地輸入 → **長按選點 + Google Maps 分享連結**

長按地圖選點已經做好了。分享連結那半還沒做：跟隨重導向取座標，
**絕不查 Google 的搜尋端點**（ADR-0008 的界線）。台灣人本來就習慣在 LINE
傳 Google Maps 連結，這條路貼合既有習慣而不是要求使用者改習慣。

## D2 — 地圖方向 → **兩種都要，畫面上有按鈕切換**

車頭朝上與北方朝上都保留，預設維持北方朝上（現有行為，改預設要真機才知道好壞）。
手動旋轉手勢仍然關閉 —— 騎乘中誤觸把方向轉歪比沒有旋轉更糟。

## D3 — 算不出路線 → **沒有「到不了的地方」**

使用者指出 Google Maps 的做法：目的地不在路上時，導到**離它最近的路上的點**，
剩下那段畫成虛線。所以「不連通」這個錯誤類別根本不該存在 ——
它是我們自己想出來的問題。

要做的是：目的地先吸附到最近的可行駛點，路線算到那裡，
**最後一段用虛線畫**，並且明說「剩下 XX 公尺請自行前往」。

## D4 + D6 — 禁止左轉必須繞過 → **自建機車專用圖磚**

使用者的原話：「要讓導航變聰明應該是要所有方面都考慮到，導航還是需要繞過禁止左轉」、
「要自建圖磚 這樣才是機車專用的圖磚」。

這推翻了我「先只播報、以後再說」的建議，而且推翻得對：
**回報如果改變不了路線，「讓導航變聰明」就只是一句話。**

寫成 [ADR-0017](docs/adr/0017-build-our-own-routing-tiles.md)。
`prohibited_segments.way_ids` 因此有用途了 —— 它是注入自訂標籤時的鍵。

## D5 — 播報 → **五秒那則預合成，二十秒那則帶路名走即時**

你說「都預合成」，但拿到路名之後這條要修一次 —— **因為兩者衝突**：
預合成要句子有限，而路名讓句子變無限（臺北 5,279 個路名）。

解法是兩則各走各的，而它們的性質本來就不同：

| | 內容 | 語音 | 為什麼 |
| --- | --- | --- | --- |
| 20 秒主指示 | 「前方 300 公尺，忠孝東路左轉」 | 即時 TTS | 它要回答「是**哪個**路口」，那非路名不可。而 20 秒吸收得了 2.8–3.6 秒的合成延遲 |
| 5 秒確認短句 | 「這裡左轉」 | **預合成** | 它要回答「就是**現在**」。這是唯一有硬期限的一則，而固定句子才預合成得了 |

其餘固定句（左轉規定 6 句、測速 16 句、禁行路段 4 句）照你的意思**全部預合成**，
順便解掉「有些警示比較小聲」那個響度不一致的問題。

「會不會走鐘」的答案在下面 —— 結論是**會走鐘的是拼接不是預合成**，
而我們一句話一個檔，不拼接。

## D7 — 路名 → **從我們自己的管線來，不從路線引擎來**

見下面「為什麼拿不到路名」。

---

# 兩個問題的答案

## 為什麼拿不到路名（D7）

**不是 BRouter 選擇不給，是它的檔案格式放不下。**

我打開 `lookups.dat` 看了。它是一份**標籤字典**，每個標籤列出它可能的值與出現次數：

```
---context:way
highway;0029035962 residential
highway;0010319731 service
highway;0007688809 track
```

圖磚裡存的不是文字，是「第幾號標籤、第幾號值」這樣的一對索引。
**這只對「值可以列舉」的標籤成立** —— `highway` 只有幾十種值、`bridge` 只有幾種。
而路名幾乎每一條都不一樣，字典會跟原始資料一樣大，
而那正是他們把圖磚壓到 11–21 MB 的手段。整份 `lookups.dat` 裡
**`name` 的條目數是 0**。

**所以自建圖磚也解不了這一題** —— 限制在格式，不在工具鏈。

**但路名並不難拿，只是要從別的地方拿。** 我們的管線本來就在向 Overpass 抓
帶路名的道路幾何（`taipei_geocode.py` 整個模組就是幹這件事的）。
把「座標 + 方位角 → 路名」建成一張表放進種子庫，就完全不必依賴路線引擎。
臺北市有名字的道路 25,534 條，這個量級的索引不大。

**而且自建圖磚之後，這件事變成順手的** —— 那時我們本來就要把 OSM 原始資料
跑一遍，路名索引是同一趟的產物。

## 會不會語音走鐘（D5）

**會走鐘的是拼接，不是預合成。**

如果做成「前方」+「300」+「公尺」+「左轉」四個音檔接起來播，那一定難聽 ——
每一段的語調都是獨立句子的語調，接起來會像四個人輪流講話，而且銜接處
的停頓長度不對。

**整句預合成沒有這個問題**，因為引擎看到的就是完整的一句話，
語調、輕重、停頓都是它自己算的 —— 與即時 TTS 唸出來的**完全一樣**，
差別只在它是提前唸好的。

所以規則只有一條：**一句話一個檔，永遠不拼接。**

句子總共幾句：

| 類別 | 數量 |
| --- | --- |
| 轉向主指示（5 個距離級距 × 左右） | 10 |
| 轉向確認短句（這裡左轉／右轉） | 2 |
| 左轉規定（待轉、直接、內側、外側、禁止、不明） | 6 |
| 測速（速限 7–8 種 × 超速與否） | 約 16 |
| 全面禁行路段（目前 4 段，各一句） | 4 |
| **合計** | **約 38 句** |

一句短音檔約幾十 KB，全部加起來一兩 MB。首次啟動合成一次，之後都是放檔案。

**兩件要注意的**：

1. **音量要一致。** 現在測速走即時 TTS、待轉走預合成檔，兩者的響度可能差一截，
   而騎士會以為是「有些警示比較小聲」。全部改成預合成正好把這個問題一起解掉。
2. **`AlertPhrases.VERSION` 是快取的鑰匙。** 改任何一句文案就要動它，
   否則裝置上留著舊音檔 —— 而症狀是「講的跟畫面上寫的不一樣」。

---

# 還沒定案

## D8 — 自建圖磚要放在哪裡（**新的，D4 的直接後果**）

自建圖磚代表**我們要自己 host 那 33 MB**，而且要定期重建（跟隨 OSM）。
這與 ADR-0008 的「每月固定成本 NT$0」直接衝突 —— 靜態檔案的流量不是零。

| | 代價 |
| --- | --- |
| A. GitHub Releases | 免費、有 CDN、我們本來就在用 GitHub。但它不是設計來當 CDN 的，量大會被關切 |
| B. Cloudflare R2 / Pages | 免費額度大，出流量免費（R2 沒有 egress 費用）。要開一個帳號與一條發布流程 |
| C. 繼續用 brouter.de 的官方圖磚 | 零成本零維護，但**那就沒有自訂標籤**，等於 D4 沒解 |

**我的建議是 B。** R2 的零 egress 正好適合「檔案大、更新少、下載多」這種形狀。

## D9 — 圖磚多久重建一次

OSM 每天在變，但機車規則不會。重建太頻繁是白費工，太少則新路不會出現。
BRouter 官方是每週。**我的建議是每季**，與 ADR-0008 原本對圖磚的預期一致，
而規則資料走另一條通道、可以當天更新。
